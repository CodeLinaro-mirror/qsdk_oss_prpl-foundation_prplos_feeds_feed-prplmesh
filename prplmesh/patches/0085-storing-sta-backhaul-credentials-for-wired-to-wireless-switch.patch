From c527f63cc6eb954eefa739690e2f0dceb758b6ba Mon Sep 17 00:00:00 2001
From: Nirmal Raj S <nirm@qti.qualcomm.com>
Date: Mon, 1 Dec 2025 11:30:04 +0530
Subject: [PATCH] storing sta backhaul credentials for wired to wireless

Signed-off-by: Nirmal Raj S <nirm@qti.qualcomm.com>
---
 .../backhaul_manager/backhaul_manager.cpp     | 116 +++++++++++++++---
 .../slave/backhaul_manager/backhaul_manager.h |   8 ++
 .../beerocks/bwl/include/bwl/sta_wlan_hal.h   |   2 +
 .../bwl/nl80211/sta_wlan_hal_nl80211.cpp      | 101 +++++++++++++++
 .../bwl/nl80211/sta_wlan_hal_nl80211.h        |   5 +
 .../platform/bpl/cfg/uci/bpl_cfg_wifi.cpp     |  56 +++++++++
 framework/platform/bpl/include/bpl/bpl_cfg.h  |   3 +
 7 files changed, 272 insertions(+), 19 deletions(-)

diff --git a/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.cpp b/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.cpp
index 1ded4829a..8dd183bff 100644
--- a/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.cpp
+++ b/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.cpp
@@ -866,7 +866,27 @@ bool BackhaulManager::backhaul_fsm_main(bool &skip_select)
             }
         }
 
-        if ((!db->device_conf.local_gw) && wired_backhaul && !m_backhaul_switch) {
+        // This snippet is commented out since the only place that use it, is also commented out.
+        // An event-driven solution will be implemented as part of the task:
+        // [TASK] Dynamic switching between wired and wireless
+        // https://github.com/prplfoundation/prplMesh/issues/866
+        // auto db = AgentDB::get();
+
+        if (!db->device_conf.local_gw) {
+            if (db->ethernet.wan.iface_name.empty()) {
+                LOG(WARNING) << "WAN interface is empty on Repeater platform configuration!";
+            }
+            eth_link_poll_timer = std::chrono::steady_clock::now();
+            m_eth_link_up       = beerocks::net::network_utils::linux_iface_is_up_and_running(
+                db->ethernet.wan.iface_name);
+            LOG(DEBUG) << "WIRED_BACKHAUL_INTERFACE STATE:" << m_eth_link_up << " SELECTED BACKHAUL: " << db->backhaul.connection_type;
+        }
+
+        // Added logic to disconnect the STA interface when backhaul_wired_iface is UP:
+        // - If already in wired_backhaul FSM: simply disconnect the STA interface.
+        // - If currently in wireless_backhaul FSM: disconnect the STA interface and transition to wired_backhaul FSM.
+        if (((!db->device_conf.local_gw) && wired_backhaul && !m_backhaul_switch) ||
+           ((!db->device_conf.local_gw) && (!wired_backhaul) && (m_eth_link_up))) {
             for (auto &radio_info : m_radios_info) {
                 auto radio = db->radio(radio_info->sta_iface);
                 if (!radio) {
@@ -878,10 +898,15 @@ bool BackhaulManager::backhaul_fsm_main(bool &skip_select)
 
                     // Enable all AP VAPS in the radio
                     send_slaves_enable();
+
+                    // Mark the connection as WIRED
+                    db->backhaul.connection_type     = AgentDB::sBackhaul::eConnectionType::Wired;
+                    db->backhaul.selected_iface_name = db->ethernet.wan.iface_name;
+                    LOG(DEBUG) << " Moved to WIRED BACKHAUL";
                 }
             }
-        } 
-
+        }
+ 
         finalize_slaves_connect_state(true);
         stop_on_failure_attempts = configuration_stop_on_failure_attempts;
 
@@ -897,20 +922,6 @@ bool BackhaulManager::backhaul_fsm_main(bool &skip_select)
          */
         m_task_pool.send_event(eTaskType::TOPOLOGY, TopologyTask::eEvent::AGENT_DEVICE_INITIALIZED);
 
-        // This snippet is commented out since the only place that use it, is also commented out.
-        // An event-driven solution will be implemented as part of the task:
-        // [TASK] Dynamic switching between wired and wireless
-        // https://github.com/prplfoundation/prplMesh/issues/866
-        // auto db = AgentDB::get();
-
-        if (!db->device_conf.local_gw) {
-            if (db->ethernet.wan.iface_name.empty()) {
-                LOG(WARNING) << "WAN interface is empty on Repeater platform configuration!";
-            }
-            eth_link_poll_timer = std::chrono::steady_clock::now();
-            m_eth_link_up       = beerocks::net::network_utils::linux_iface_is_up_and_running(
-                db->ethernet.wan.iface_name);
-        }
         m_backhaul_switch = false;
         FSM_MOVE_STATE(OPERATIONAL);
         break;
@@ -950,10 +961,18 @@ bool BackhaulManager::backhaul_fsm_main(bool &skip_select)
             eth_link_poll_timer = now;
             bool eth_link_up = beerocks::net::network_utils::linux_iface_is_up_and_running(db->ethernet.wan.iface_name);
             if (eth_link_up != m_eth_link_up) {
+                //update the previous bh connection
+                if (m_eth_link_up) {
+                    m_prev_bh_connection = backhaul_connection::Wired;
+                } else {
+                    m_prev_bh_connection = backhaul_connection::Wireless;
+                    m_wireless_bh = true;
+                } 
                 m_eth_link_up = beerocks::net::network_utils::linux_iface_is_up_and_running(db->ethernet.wan.iface_name);
 
-                FSM_MOVE_STATE(RESTART);
                 m_backhaul_switch = true;
+        
+                stop_on_failure_attempts = configuration_stop_on_failure_attempts;
 
                 // remove sta connection while switching from wireless to wired
                 if (m_eth_link_up) {
@@ -972,6 +991,7 @@ bool BackhaulManager::backhaul_fsm_main(bool &skip_select)
                         }
                     }
                 }
+                FSM_MOVE_STATE(RESTART);
             }
         }
         }
@@ -1267,7 +1287,16 @@ bool BackhaulManager::backhaul_fsm_wireless(bool &skip_select)
                 }
                 // reconnect backhaul sta if backhaul switching is happened already  
                 if (m_backhaul_switch || radio_info->sta_wlan_hal->is_disconnected()) {
-                    radio_info->sta_wlan_hal->reconnect_backhaul_sta();
+                    // already wireless connection established
+                    if (m_wireless_bh) {
+                        radio_info->sta_wlan_hal->reconnect_backhaul_sta();
+                    } else {
+                        // wired to wireless first time switching - add bh credentials
+                        if ((!radio_info->add_network_status) && radio_info->sta_wlan_hal->add_new_network(radio_info->bSta_conf)) {
+                            LOG(DEBUG) << " ADD NETWORK Completed ";
+                            radio_info->add_network_status = true;
+                        }
+                    }
                     m_backhaul_switch = false;
                 } else if (!roam_flag && radio_info->sta_wlan_hal->is_connected()) {
                     for (const auto &sta_iface : slave_sta_ifaces) {
@@ -1887,6 +1916,51 @@ bool BackhaulManager::handle_slave_backhaul_message(int fd, ieee1905_1::CmduMess
 
         break;
     }
+    case beerocks_message::ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST: {
+        LOG(TRACE) << "received ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST";
+         auto db = AgentDB::get();
+
+        bool wired_backhaul =
+            db->backhaul.connection_type == AgentDB::sBackhaul::eConnectionType::Wired;
+
+        auto request =
+            beerocks_header
+                ->addClass<beerocks_message::cACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST>();
+        if (!request) {
+            LOG(ERROR) << "ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST failed";
+            return false;
+        }
+
+        const sMacAddr &radio_mac = request->radio_mac();
+
+        LOG(DEBUG) << "handle ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST for bsta " << radio_mac;
+        if ((!db->device_conf.local_gw) && wired_backhaul && (m_prev_bh_connection != backhaul_connection::Wireless)) {
+
+            for (auto &radio_info : m_radios_info) {
+                std::string iface = radio_info->sta_iface;
+                if (radio_info->sta_iface.empty()) {
+                    LOG(DEBUG) << " STA Iface is empty for radio: " << radio_mac << " skip ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST " ;
+                    continue;
+                }
+                if (radio_info->radio_mac == radio_mac) {
+                    LOG(DEBUG) << " Radio matched ";
+                    auto config_data = request->wifi_credentials();
+                    if (config_data) {
+                        radio_info->bSta_conf.ssid                = config_data->ssid_str();
+                        radio_info->bSta_conf.authentication_type = config_data->authentication_type_attr().data;
+                        radio_info->bSta_conf.encryption_type     = config_data->encryption_type_attr().data;
+                        radio_info->bSta_conf.network_key         = config_data->network_key_str();
+                        LOG(DEBUG) << "BACKHUAL_MANAGER: Received For radio: " << radio_mac << " BSTA Cred: SSID-" << radio_info->bSta_conf.ssid
+                                   << " auth:" << radio_info->bSta_conf.authentication_type 
+                                   << " encr:" << radio_info->bSta_conf.encryption_type;
+                    }
+                }
+            }
+        } else {
+            LOG(DEBUG) << " Skip ACTION_BACKHAUL_WIFI_CREDENTIALS_UPDATE_REQUEST " ;
+        }
+        break;
+    }
     default: {
         bool handled =
             m_task_pool.handle_cmdu(cmdu_rx, 0, sMacAddr(), sMacAddr(), fd, beerocks_header);
@@ -2194,6 +2268,10 @@ bool BackhaulManager::hal_event_handler(bwl::base_wlan_hal::hal_event_ptr_t even
     } break;
 
     case Event::Disconnected: {
+        if ((m_prev_bh_connection == backhaul_connection::Wireless) && (m_backhaul_switch)) {
+            LOG(DEBUG) << " Skip Disconnected event during wireless to wired backhaul switching" ;
+            return true;
+        }
         if (FSM_IS_IN_STATE(WAIT_WPS)) {
             return true;
         }
diff --git a/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.h b/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.h
index 4d95ec833..8439de120 100644
--- a/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.h
+++ b/agent/src/beerocks/slave/backhaul_manager/backhaul_manager.h
@@ -381,6 +381,12 @@ private:
     bool m_eth_link_up  = false;
     bool m_backhaul_switch  = false;
 
+    enum class backhaul_connection { Invalid = -1, Wireless, Wired};
+
+    backhaul_connection m_prev_bh_connection = backhaul_connection::Invalid;
+
+    bool m_wireless_bh = false;
+
     std::string bssid_bridge_mac;
 
     std::unique_ptr<beerocks::agent_ucc_listener> m_agent_ucc_listener;
@@ -433,6 +439,8 @@ public:
         std::shared_ptr<bwl::sta_wlan_hal> sta_wlan_hal;
         std::vector<int> sta_hal_ext_events = {beerocks::net::FileDescriptor::invalid_descriptor};
         int sta_hal_int_events              = beerocks::net::FileDescriptor::invalid_descriptor;
+        son::wireless_utils::sBssInfoConf bSta_conf;
+        bool add_network_status = false;
     };
 
 private:
diff --git a/common/beerocks/bwl/include/bwl/sta_wlan_hal.h b/common/beerocks/bwl/include/bwl/sta_wlan_hal.h
index d93571cf7..350d9ab25 100644
--- a/common/beerocks/bwl/include/bwl/sta_wlan_hal.h
+++ b/common/beerocks/bwl/include/bwl/sta_wlan_hal.h
@@ -102,6 +102,8 @@ public:
     virtual bool reconnect_backhaul_sta() = 0;
 
     virtual bool is_disconnected()  = 0;
+
+    virtual bool add_new_network(son::wireless_utils::sBssInfoConf bSta_conf) = 0;
 };
 
 // STA HAL factory types
diff --git a/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.cpp
index 65df9de0f..8523a5e2a 100644
--- a/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.cpp
@@ -546,6 +546,107 @@ bool sta_wlan_hal_nl80211::is_disconnected()
     return (connection_status.wpa_state.compare("DISCONNECTED") == 0);
 }
 
+bool sta_wlan_hal_nl80211::add_new_network(son::wireless_utils::sBssInfoConf bSta_conf)
+{
+    std::string sta_iface = get_iface_name();
+
+    LOG(INFO) << "Add a new network for interface: " << sta_iface;
+    // Add a new network
+    auto network_id = add_network();
+    if (network_id < 0) {
+        LOG(ERROR) << "Failed (" << network_id
+                   << ") adding new network to interface: " << sta_iface;
+        return false;
+    }
+    // Set SSID
+    if (!set_network(network_id, "ssid", "\"" + bSta_conf.ssid + "\"")) {
+        LOG(ERROR) << "Failed setting ssid on interface " << sta_iface;
+        return false;
+    }
+
+    // Security and Password
+    std::string key_mgmt = "";
+    std::string psk = "";
+    std::string group = "CCMP";
+    std::string pairwise = "CCMP";
+
+    if (bSta_conf.authentication_type == WSC::eWscAuth::WSC_AUTH_WPA2PSK) {
+        key_mgmt = "WPA-PSK";
+        psk = "psk";
+    } else if (bSta_conf.authentication_type == WSC::eWscAuth::WSC_AUTH_SAE) {
+        key_mgmt = "SAE";
+        psk = "sae_password";
+        if (!set_network(network_id, "ieee80211w", "2")) {
+            LOG(ERROR) << "Failed setting ieee80211w on interface " << sta_iface;
+            return false;
+        }
+        if (!set_network(network_id, "sae_pwe", "1")) {
+            LOG(ERROR) << "Failed setting sae_pwe on interface " << sta_iface;
+            return false;
+        }
+    } else if (bSta_conf.authentication_type == WSC::eWscAuth(WSC::eWscAuth::WSC_AUTH_WPA2PSK|WSC::eWscAuth::WSC_AUTH_SAE)) {
+        key_mgmt = "WPA-PSK SAE";
+        psk = "sae_password";
+        if (!set_network(network_id, "ieee80211w", "2")) {
+            LOG(ERROR) << "Failed setting ieee80211w on interface " << sta_iface;
+            return false;
+        }
+        if (!set_network(network_id, "sae_pwe", "1")) {
+            LOG(ERROR) << "Failed setting sae_pwe on interface " << sta_iface;
+            return false;
+        }
+    } else {
+        LOG(ERROR) << " Unsupported auth type: " << bSta_conf.authentication_type;
+        return false;
+    }
+
+    if (!set_network(network_id, "key_mgmt", key_mgmt)) {
+        LOG(ERROR) << "Failed setting key_mgmt on interface " << sta_iface;
+        return false;
+    }
+
+    if ((!bSta_conf.network_key.empty()) && (!set_network(network_id, "psk", "\"" + bSta_conf.network_key + "\""))) {
+        LOG(ERROR) << "Failed setting psk on interface " << sta_iface;
+        return false;
+    }
+
+    if (!set_network(network_id, "pairwise",pairwise)) {
+        LOG(ERROR) << "Failed setting pairwise on interface " << sta_iface;
+        return false;
+    }
+    if (!set_network(network_id, "group",group)) {
+        LOG(ERROR) << "Failed setting group on interface " << sta_iface;
+        return false;
+    }
+    
+    if (!set_network(network_id, "multi_ap_backhaul_sta","1")) {
+        LOG(ERROR) << "Failed setting multi_ap_backhaul_sta on interface " << sta_iface;
+        return false;
+    }
+
+
+    // Enable the network
+    if (!enable_network(network_id)) {
+        LOG(ERROR) << "Failed enabling network id = " << network_id
+                   << " on interface: " << sta_iface;
+        return false;
+    }
+
+    // select the network
+    const std::string cmd = "SELECT_NETWORK " + std::to_string(network_id);
+    if (!wpa_ctrl_send_msg(cmd)) {
+        LOG(ERROR) << "WPA command '" << cmd << "' failed";
+        return false;
+    }
+
+
+    LOG(DEBUG) << "Network with id " << network_id << " has been added and enabled on interface "
+               << sta_iface;
+
+    beerocks::bpl::bpl_cfg_set_sta_vap_bh_credentials(sta_iface,bSta_conf);
+ 
+    return true;
+}
 
 } // namespace nl80211
 
diff --git a/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.h b/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.h
index 8987bddd0..c039761b8 100644
--- a/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.h
+++ b/common/beerocks/bwl/nl80211/sta_wlan_hal_nl80211.h
@@ -11,6 +11,7 @@
 
 #include "base_wlan_hal_nl80211.h"
 #include <bwl/sta_wlan_hal.h>
+#include <bpl/bpl_cfg.h>
 
 #define BUFFER_SIZE 4096
 
@@ -73,6 +74,8 @@ public:
     virtual bool disconnect_backhaul_sta() override;
     virtual bool reconnect_backhaul_sta() override;
     virtual bool is_disconnected() override;
+    
+    virtual bool add_new_network(son::wireless_utils::sBssInfoConf bSta_conf) override;
 
 protected:
     virtual bool process_nl80211_event(parsed_obj_map_t &parsed_obj) override;
@@ -255,6 +258,8 @@ private:
     std::chrono::steady_clock::time_point m_unassoc_measure_start;
     int m_unassoc_measure_window_size = 0;
     int m_unassoc_measure_delay       = 0;
+
+    son::wireless_utils::sBssInfoConf m_bSta_conf;
 };
 
 } // namespace nl80211
diff --git a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
index 646b90d7d..36f392868 100644
--- a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
+++ b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
@@ -897,6 +897,62 @@ bool bpl_cfg_get_monitored_BSSs_by_radio_iface(const std::string &iface,
     // break monitor vaps to vector
 }
 
+bool bpl_cfg_set_sta_vap_bh_credentials(const std::string &iface,
+                                        const son::wireless_utils::sBssInfoConf &configuration)
+{
+    // Find the "wireless.wifi-iface" section in UCI configuration for the given interface
+    const std::string package_name = "wireless";
+    const std::string section_type = "wifi-iface";
+    const std::string option_name  = "ifname";
+    std::string section_name;
+    if (!uci_find_section_by_option(package_name, section_type, option_name, iface, section_name)) {
+	LOG(ERROR) << "Failed to find configuration section for interface " << iface;
+        return false;
+    }
+    if (section_name.empty()) {
+	LOG(ERROR) << "section_name is empty for interface " << iface;
+        return false;
+    }
+    // Overwrite UCI configuration with wireless credentials for the given interface
+    OptionsUnorderedMap options;
+    options["ssid"] = configuration.ssid;
+    
+    auto get_encryption = [](WSC::eWscAuth authentication_type, WSC::eWscEncr encryption_type) {
+            std::string encryption = "none";
+            if (authentication_type == WSC::eWscAuth::WSC_AUTH_WPA2PSK) {
+        	    encryption = "psk2";
+        	    if (encryption_type == WSC::eWscEncr::WSC_ENCR_TKIP) {
+        		    encryption += "+tkip";
+        	    } else if (encryption_type == WSC::eWscEncr::WSC_ENCR_AES) {
+        		    encryption += "+aes";
+        	    }
+            } else if (authentication_type == WSC::eWscAuth::WSC_AUTH_SAE) {
+        	    encryption = "sae";
+            } else if (authentication_type == (WSC::eWscAuth::WSC_AUTH_SAE|WSC::eWscAuth::WSC_AUTH_WPA2PSK)) {
+        	    encryption = "sae-mixed";
+            }
+            return encryption;
+    };
+    options["encryption"] = get_encryption(configuration.authentication_type, configuration.encryption_type);
+
+    // Set sae_pwe if encryption is SAE
+    if (options["encryption"] == "sae") {
+    	    options["sae_pwe"] = "2";
+        options["sae"] = "1";
+    }
+    if (options["encryption"] == "sae-mixed") {
+                options["sae_pwe"] = "2";
+    }
+    options["key"] = configuration.network_key;
+    // Write UCI options in the "wireless.wifi-iface" section for the given interface.
+    if (!uci_set_section(package_name, section_type, section_name, options, true)) {
+        LOG(ERROR) << "Failed to set wireless configuration for interface " << iface << " at section " << section_name;
+        return false;
+    }
+
+    LOG(DEBUG) << "Successfully added the bh  credentials for sta interface " << iface << " at section " << section_name;
+    return true;
+}
 #endif
 
 } // namespace bpl
diff --git a/framework/platform/bpl/include/bpl/bpl_cfg.h b/framework/platform/bpl/include/bpl/bpl_cfg.h
index 9c0e150c5..b0d487e78 100644
--- a/framework/platform/bpl/include/bpl/bpl_cfg.h
+++ b/framework/platform/bpl/include/bpl/bpl_cfg.h
@@ -1263,6 +1263,9 @@ bool cfg_update_prplmesh_option(const std::string &package_name,
                             const std::string &old_value,
                             const std::string &new_value);
 
+bool bpl_cfg_set_sta_vap_bh_credentials(const std::string &iface,
+                                  const son::wireless_utils::sBssInfoConf &configuration);
+
 } // namespace bpl
 } // namespace beerocks
 
-- 
2.34.1

