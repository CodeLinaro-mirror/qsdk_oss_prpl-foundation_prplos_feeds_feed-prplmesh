From aa36ab35a33daad6125ff6b90b38c8727d7fd005 Mon Sep 17 00:00:00 2001
From: Girija P <gpandiya@qti.qualcomm.com>
Date: Wed, 15 Oct 2025 10:56:13 +0530
Subject: [PATCH] [UDMesh/QMesh] [NL HAL] Link remove via bml_cli support

Signed-off-by: Girija P <gpandiya@qti.qualcomm.com>
---
 .../ap_manager/ap_manager.cpp                 |   2 +-
 .../bwl/nl80211/ap_wlan_hal_nl80211.cpp       | 370 ++++++++++--------
 .../bwl/nl80211/ap_wlan_hal_nl80211.h         |   3 +
 .../bwl/nl80211/base_wlan_hal_nl80211.cpp     |  27 +-
 4 files changed, 226 insertions(+), 176 deletions(-)

diff --git a/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp b/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
index 48af7825f..228780655 100644
--- a/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
+++ b/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
@@ -2260,7 +2260,7 @@ void ApManager::handle_cmdu(ieee1905_1::CmduMessageRx &cmdu_rx)
         break;
     }
     case beerocks_message::ACTION_APMANAGER_AFFILIATED_AP_REMOVE_REQUEST: {
-	LOG(DEBUG) << "Received ACTION_APMANAGER_AFFILIATED_AP_REMOVE_REQUEST";
+    	LOG(DEBUG) << "Received ACTION_APMANAGER_AFFILIATED_AP_REMOVE_REQUEST";
         auto msg = beerocks_header->addClass<beerocks_message::cACTION_APMANAGER_AFFILIATED_AP_REMOVE_REQUEST>();
         if (!msg) {
             LOG(ERROR) << "addClass cACTION_APMANAGER_AFFILIATED_AP_REMOVE_REQUEST has failed";
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
index 379a2aa75..b99024d57 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
@@ -1153,15 +1153,15 @@ void set_wifi_credentials_from_vap(const std::string &vap_id,
 }
 
 bool ap_wlan_hal_nl80211::update_vap_credentials(
-    std::list<son::wireless_utils::sBssInfoConf> &bss_info_conf_list,
-    const std::string &backhaul_wps_ssid, const std::string &backhaul_wps_passphrase,
-    const std::string &bridge_ifname)
+        std::list<son::wireless_utils::sBssInfoConf> &bss_info_conf_list,
+        const std::string &backhaul_wps_ssid, const std::string &backhaul_wps_passphrase,
+        const std::string &bridge_ifname)
 {
     LOG(INFO) << "Update vap credentials: " << m_radio_info.iface_name<<" freq of radio is: " << m_radio_info.frequency_band;
-    
+
     std::string base_interface="";
     extractLinkBaseInterface(m_radio_info.iface_name, base_interface);
- 
+
     // Load hostapd config for the radio
     prplmesh::hostapd::Configuration base_conf = load_hostapd_config(m_radio_info.iface_name,"");
     if (!base_conf) {
@@ -1214,8 +1214,8 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
         LOG(DEBUG) << "mld_id:          " << bss_it.mld_id;
         int op_class_it = 0;
         for (uint8_t op_class : bss_it.operating_class) {
-                op_class_it++;
-                LOG(DEBUG) << "op_class_list[" << op_class_it << "]: " << static_cast<int>(op_class);
+            op_class_it++;
+            LOG(DEBUG) << "op_class_list[" << op_class_it << "]: " << static_cast<int>(op_class);
         }
         LOG(DEBUG) << "fronthaul:       " << bss_it.fronthaul;
         LOG(DEBUG) << "backhaul:        " << bss_it.backhaul;
@@ -1234,7 +1234,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             LOG(INFO) << "create new vap for wildcard bssid ";
 
             std::string new_vap_name = base_interface + "_" + std::to_string(new_vap_index);
-            
+
             sMacAddr new_bssid;
 
             ap_wlan_hal_nl80211::generate_new_bssid(new_vap_index,new_bssid);
@@ -1282,7 +1282,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             if (!m_radio_info.hostapd_new_bss_config.empty()) {
                 auto& new_vap = m_radio_info.hostapd_new_bss_config.front();
                 std::vector<std::string>& config_lines = new_vap.second;
-            
+
                 for (auto& line : config_lines) {
                     if (line.rfind("interface=", 0) == 0) { // starts with "interface="
                         line = "bss=" + new_vap.first;      // replace with "bss=<vap_name>"
@@ -1310,34 +1310,17 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             }
 
             auto &vap_id = conf.get_vap_by_bssid(tlvf::mac_to_string(bss_it.bssid));
-                LOG(INFO) << "vap_id: " << vap_id;
+            LOG(INFO) << "vap_id: " << vap_id;
             if (vap_id.empty()) {
                 LOG(ERROR) << " Get vap id failed " ;
                 continue; 
             }       
 
-            // escape I
-            auto auth_type = son::wireless_utils::wsc_to_bwl_authentication(bss_it.authentication_type);
-            if (auth_type == "INVALID") {
-                LOG(ERROR) << "Autoconfiguration: auth type is 'INVALID'; number: "
-                           << (uint16_t)bss_it.authentication_type;
-                abort = true;
-                break;
-            }
-
-            // escape II
-            auto enc_type = son::wireless_utils::wsc_to_bwl_encryption(bss_it.encryption_type);
-            if (enc_type == "INVALID") {
-                LOG(ERROR) << "Autoconfiguration: enc_type is 'INVALID'; number: "
-                           << int(bss_it.encryption_type);
-                abort = true;
-                break;
-            }
 
             // escape III
             std::string bss_key_name;
             std::string bss   = conf.get_vap_value(vap_id, "bss"),
-                        bssid = conf.get_vap_value(vap_id, "bssid");
+                bssid = conf.get_vap_value(vap_id, "bssid");
             if (bss.empty()) {
                 bss = conf.get_vap_value(vap_id,"interface");
                 bss_key_name = "interface";
@@ -1345,8 +1328,8 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                 bss_key_name = "bss";
             }
 
-	    bss_it.old_iface_name = bss;
-	    LOG(INFO) << "iface name before changing: " << bss_it.old_iface_name;
+            bss_it.old_iface_name = bss;
+            LOG(INFO) << "iface name before changing: " << bss_it.old_iface_name;
 
             // check explicit bssid value only in case of Multiple BSSID support
             if ((!bss.empty()) && bssid.empty()) {
@@ -1355,114 +1338,134 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                 break;
             }
 
+            if (!bss_it.teardown) {
 
-            if (!assign_auth_encr_parameters(conf, vap_id, bss_it)) {
-                LOG(ERROR) << "Failed to set the authentication/encryption parameters!";
-                abort = true;
-                break;
-            }
+                // escape I
+                auto auth_type = son::wireless_utils::wsc_to_bwl_authentication(bss_it.authentication_type);
+                if (auth_type == "INVALID") {
+                    LOG(ERROR) << "Autoconfiguration: auth type is 'INVALID'; number: "
+                        << (uint16_t)bss_it.authentication_type;
+                    abort = true;
+                    break;
+                }
 
-            if (!bridge_ifname.empty() &&
-                beerocks::net::network_utils::linux_iface_is_up(bridge_ifname)) {
-                conf.set_create_vap_value(vap_id, "bridge", bridge_ifname);
-            }
+                // escape II
+                auto enc_type = son::wireless_utils::wsc_to_bwl_encryption(bss_it.encryption_type);
+                if (enc_type == "INVALID") {
+                    LOG(ERROR) << "Autoconfiguration: enc_type is 'INVALID'; number: "
+                        << int(bss_it.encryption_type);
+                    abort = true;
+                    break;
+                }
 
 
-            LOG(DEBUG) << "Autoconfiguration for ssid: " << bss_it.ssid << " auth_type: " << auth_type
-                       << " encr_type: " << enc_type << " network_key: " << bss_it.network_key
-                       << " fronthaul: " << beerocks::string_utils::bool_str(bss_it.fronthaul)
-                       << " backhaul: " << beerocks::string_utils::bool_str(bss_it.backhaul);
-            
-            if (!ieee80211be_sprt.empty()) {
+                if (!assign_auth_encr_parameters(conf, vap_id, bss_it)) {
+                    LOG(ERROR) << "Failed to set the authentication/encryption parameters!";
+                    abort = true;
+                    break;
+                }
+
+                if (!bridge_ifname.empty() &&
+                        beerocks::net::network_utils::linux_iface_is_up(bridge_ifname)) {
+                    conf.set_create_vap_value(vap_id, "bridge", bridge_ifname);
+                }
+
+
+                LOG(DEBUG) << "Autoconfiguration for ssid: " << bss_it.ssid << " auth_type: " << auth_type
+                    << " encr_type: " << enc_type << " network_key: " << bss_it.network_key
+                    << " fronthaul: " << beerocks::string_utils::bool_str(bss_it.fronthaul)
+                    << " backhaul: " << beerocks::string_utils::bool_str(bss_it.backhaul);
+
+                if (!ieee80211be_sprt.empty()) {
                     conf.set_create_vap_value(vap_id, "mld_ap","1");
-             }
-
-            conf.set_create_vap_value(vap_id, "ssid", bss_it.ssid);
-            if(!m_radio_info.wps_pushbutton_configured && bss_it.fronthaul){
-                conf.set_create_vap_value(vap_id, "wps_state", bss_it.fronthaul ? "2" : "");
-                conf.set_create_vap_value(vap_id, "wps_independent", bss_it.fronthaul ? "1" : "");
-                conf.set_create_vap_value(vap_id, "config_methods","push_button");
-                bss_it.wps_pushbutton = true;
-                LOG(INFO) << "wps_pushbutton set to: " << bss_it.wps_pushbutton;
-                m_radio_info.wps_pushbutton_configured=true;
-            }
-            
-            std::string multi_ap;
-            if (bss_it.fronthaul) {
-                if (bss_it.backhaul) {
-                    multi_ap = "3";
-                } else {
-                    multi_ap = "2";
                 }
-            } else {
-                if (bss_it.backhaul) {
-                    multi_ap = "1";
+
+                conf.set_create_vap_value(vap_id, "ssid", bss_it.ssid);
+                if(!m_radio_info.wps_pushbutton_configured && bss_it.fronthaul){
+                    conf.set_create_vap_value(vap_id, "wps_state", bss_it.fronthaul ? "2" : "");
+                    conf.set_create_vap_value(vap_id, "wps_independent", bss_it.fronthaul ? "1" : "");
+                    conf.set_create_vap_value(vap_id, "config_methods","push_button");
+                    bss_it.wps_pushbutton = true;
+                    LOG(INFO) << "wps_pushbutton set to: " << bss_it.wps_pushbutton;
+                    m_radio_info.wps_pushbutton_configured=true;
+                }
+
+                std::string multi_ap;
+                if (bss_it.fronthaul) {
+                    if (bss_it.backhaul) {
+                        multi_ap = "3";
+                    } else {
+                        multi_ap = "2";
+                    }
                 } else {
-                    LOG(WARNING) << "BSS configured with fronthaul nor backhaul";
-                    multi_ap = "0";
+                    if (bss_it.backhaul) {
+                        multi_ap = "1";
+                    } else {
+                        LOG(WARNING) << "BSS configured with fronthaul nor backhaul";
+                        multi_ap = "0";
+                    }
+                }
+                conf.set_create_vap_value(vap_id, "multi_ap", multi_ap);
+                //required to update uci wireless
+                bss_it.multi_ap = multi_ap;
+                // oddly enough, multi_ap_backhaul_wpa_passphrase has to be
+                // quoted, while wpa_passphrase does not...
+                if (bss_it.fronthaul && !backhaul_wps_ssid.empty()) {
+                    conf.set_create_vap_value(vap_id, "multi_ap_backhaul_ssid",
+                            "\"" + backhaul_wps_ssid + "\"");
+                    conf.set_create_vap_value(vap_id, "multi_ap_backhaul_wpa_passphrase",
+                            backhaul_wps_passphrase);
                 }
-            }
-            conf.set_create_vap_value(vap_id, "multi_ap", multi_ap);
-            //required to update uci wireless
-            bss_it.multi_ap = multi_ap;
-            // oddly enough, multi_ap_backhaul_wpa_passphrase has to be
-            // quoted, while wpa_passphrase does not...
-            if (bss_it.fronthaul && !backhaul_wps_ssid.empty()) {
-                conf.set_create_vap_value(vap_id, "multi_ap_backhaul_ssid",
-                                          "\"" + backhaul_wps_ssid + "\"");
-                conf.set_create_vap_value(vap_id, "multi_ap_backhaul_wpa_passphrase",
-                                          backhaul_wps_passphrase);
-            }
 
-            // remove when not needed
-            if (!bss_it.fronthaul && backhaul_wps_ssid.empty()) {
-                conf.set_create_vap_value(vap_id, "multi_ap_backhaul_ssid", "");
-                conf.set_create_vap_value(vap_id, "multi_ap_backhaul_wpa_passphrase", "");
-            }
+                // remove when not needed
+                if (!bss_it.fronthaul && backhaul_wps_ssid.empty()) {
+                    conf.set_create_vap_value(vap_id, "multi_ap_backhaul_ssid", "");
+                    conf.set_create_vap_value(vap_id, "multi_ap_backhaul_wpa_passphrase", "");
+                }
 
-            // we always need to get the mgmt frames (assoc req) for capability reports:
-            conf.set_create_vap_value(vap_id, "notify_mgmt_frames", "1");
+                // we always need to get the mgmt frames (assoc req) for capability reports:
+                conf.set_create_vap_value(vap_id, "notify_mgmt_frames", "1");
 
-            // finally enable the vap (remove any previously set start_disabled)
-            conf.set_create_vap_value(vap_id, "start_disabled", "");
+                // finally enable the vap (remove any previously set start_disabled)
+                conf.set_create_vap_value(vap_id, "start_disabled", "");
 
-            conf.set_create_vap_value(vap_id, "wds_sta", "1");
+                conf.set_create_vap_value(vap_id, "wds_sta", "1");
 
-            if (bss_it.mld_id >= 0) {
-                std::string new_bss_name = get_ifname_wrt_mld_grp(bss_it.mld_id, bss);
-                if (new_bss_name == bss) {
-                    LOG(INFO) << bss << ": No need to change bss name for this mld grouping";
-                } else {
-                    LOG(INFO) << bss << ": Need to change the bss name to" << new_bss_name << "for this mld grouping";
-                    bss_name_changed = true;
-                    if (conf.set_create_vap_value(vap_id, bss_key_name, new_bss_name)) {
-				        LOG(INFO) << "set interface/bss pass";
-			        } else {
-				        LOG(ERROR) << "set interface/bss fail";
-			        }
-                    auto new_bss = conf.get_vap_value(vap_id, bss_key_name);
-                    LOG(INFO) << "After setting bss value: " << new_bss;
- 
-                    bss = new_bss_name;
+                if (bss_it.mld_id >= 0) {
+                    std::string new_bss_name = get_ifname_wrt_mld_grp(bss_it.mld_id, bss);
+                    if (new_bss_name == bss) {
+                        LOG(INFO) << bss << ": No need to change bss name for this mld grouping";
+                    } else {
+                        LOG(INFO) << bss << ": Need to change the bss name to" << new_bss_name << "for this mld grouping";
+                        bss_name_changed = true;
+                        if (conf.set_create_vap_value(vap_id, bss_key_name, new_bss_name)) {
+                            LOG(INFO) << "set interface/bss pass";
+                        } else {
+                            LOG(ERROR) << "set interface/bss fail";
+                        }
+                        auto new_bss = conf.get_vap_value(vap_id, bss_key_name);
+                        LOG(INFO) << "After setting bss value: " << new_bss;
+
+                        bss = new_bss_name;
+                    }
                 }
-            }
 
-            if ((bss == base_radio_iface_name) && (new_radio_iface_name_update_req) ){
+                if ((bss == base_radio_iface_name) && (new_radio_iface_name_update_req) ){
                     new_radio_iface_name_update_req = 0;
                     LOG(INFO) << "radio_iface_name: "<< base_radio_iface_name<<" and bss: "<< bss <<"are same: Found matching radio_iface that fh manager is running with";
-            } else if ((!new_radio_iface_name_updated) && (new_radio_iface_name_update_req)) {
+                } else if ((!new_radio_iface_name_updated) && (new_radio_iface_name_update_req)) {
                     new_radio_iface_name = bss;
                     base_radio_bssid = bssid;
                     new_radio_iface_name_updated = true;
                     LOG(INFO) << "radio_iface_name: "<< base_radio_iface_name <<" and bss: "<< bss <<"are not same: Updating new_radio_iface_name if in case  radio_iface_name_update_required!";
+                }
             }
-
             // escape IV
             const auto vap_iter =
                 std::find_if(m_radio_info.available_vaps.begin(), m_radio_info.available_vaps.end(),
-                             [&bssid](const std::pair<int, bwl::VAPElement> &elem) {
-                                 return (elem.second.mac == bssid);
-                             });
+                        [&bssid](const std::pair<int, bwl::VAPElement> &elem) {
+                        return (elem.second.mac == bssid);
+                        });
             // check bssid exists in available vaps since bssids should not change.
             if (vap_iter != m_radio_info.available_vaps.end()) {
                 LOG(DEBUG) << "Vap is found in existing vaps";
@@ -1470,25 +1473,27 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                 auto &vap_info     = m_radio_info.available_vaps[vap_iter->first];
                 //vap_info.bss       = bss;
                 //vap_info.mac       = bssid;
-                vap_info.fronthaul = bss_it.fronthaul;
-                vap_info.backhaul  = bss_it.backhaul;
-                if (vap_info.backhaul) {
-                    vap_info.ssid = backhaul_wps_ssid;
-                    vap_info.profile1_backhaul_sta_association_disallowed =
-                        bss_it.profile1_backhaul_sta_association_disallowed;
-                    vap_info.profile2_backhaul_sta_association_disallowed =
-                        bss_it.profile2_backhaul_sta_association_disallowed;
-                } else {
-                    vap_info.ssid                                         = bss_it.ssid;
-                    vap_info.profile1_backhaul_sta_association_disallowed = false;
-                    vap_info.profile2_backhaul_sta_association_disallowed = false;
-                }
                 if (bss_it.teardown) {
                     // BSS is flagged for teardown
                     LOG(DEBUG) << "Disabling VAP " << vap_id;
                     conf.remove_bss_config(vap_id);
-                }
+                } else {
+
+                    vap_info.fronthaul = bss_it.fronthaul;
+                    vap_info.backhaul  = bss_it.backhaul;
+                    if (vap_info.backhaul) {
+                        vap_info.ssid = backhaul_wps_ssid;
+                        vap_info.profile1_backhaul_sta_association_disallowed =
+                            bss_it.profile1_backhaul_sta_association_disallowed;
+                        vap_info.profile2_backhaul_sta_association_disallowed =
+                            bss_it.profile2_backhaul_sta_association_disallowed;
+                    } else {
+                        vap_info.ssid                                         = bss_it.ssid;
+                        vap_info.profile1_backhaul_sta_association_disallowed = false;
+                        vap_info.profile2_backhaul_sta_association_disallowed = false;
+                    }
 
+                }
 
                 if (conf.update_required()) {
                     if (!conf.store()) {
@@ -1496,7 +1501,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                         continue;
                     }
                     //set uci persist for the particular vapid
-                    if(beerocks::bpl::cfg_is_master()==0)
+                    if(beerocks::bpl::cfg_is_master()==0 && (!bss_it.teardown)) /* set wifi credentials require mld_id that have to be populated in bss_it, extract from mld entrant file */
                     {
                         set_wifi_credentials_from_vap(vap_id, bss_it, conf);
                     }
@@ -1512,7 +1517,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                             continue;
                         }
                         int links_present = 0;
-                        
+
                         auto it = result.find("num_links");
                         if (it != result.end() && !it->second.empty()) {
                             const std::string &num_links_str = it->second;
@@ -1527,30 +1532,39 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                         } else {
                             LOG(WARNING) << "num_links not found or empty in result";
                         }
-
-                        if(links_present > 1 && links_present <= 4)
-                        {
-                            const std::string cmd("STOP_MLD");
-                            if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
-                                LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
-                                continue;
+                        if (bss_it.teardown) {
+                            if (links_present > 1) {
+                                LOG(ERROR) << "Hit remove link " << bss_it.bssid;
+                                remove_mld_link(vap_info.bss, bss_it.bssid);
+                            } else {
+                                LOG(ERROR) << "Hit remove bss " << bss_it.bssid;
+                                remove_bss(vap_info.bss, bss_it.bssid);
                             }
-                            LOG(INFO) << "STOP_MLD successful";
                         } else {
-                            const std::string cmd("STOP_AP");
-                            if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
-                                LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
+                            if(links_present > 1)
+                            {
+                                const std::string cmd("STOP_MLD");
+                                if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
+                                    LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
+                                    continue;
+                                }
+                                LOG(INFO) << "STOP_MLD successful";
+                            } else {
+                                const std::string cmd("STOP_AP");
+                                if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
+                                    LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
+                                    continue;
+                                }
+                                LOG(INFO) << "STOP_AP successful";
+                            }
+                            const std::string cmd_2("RELOAD_CONFIG");
+                            if (!wpa_ctrl_send_msg(cmd_2,vap_info.bss)) {
+                                LOG(ERROR) << "Autoconfiguration: \"" << cmd_2 << "\" command to hostapd has failed";
                                 continue;
                             }
-                            LOG(INFO) << "STOP_AP successful";
-                        }
-                        const std::string cmd_2("RELOAD_CONFIG");
-                        if (!wpa_ctrl_send_msg(cmd_2,vap_info.bss)) {
-                            LOG(ERROR) << "Autoconfiguration: \"" << cmd_2 << "\" command to hostapd has failed";
-                            continue;
+                            LOG(INFO) << "RELOAD_CONFIG successful";
                         }
-                        LOG(INFO) << "RELOAD_CONFIG successful";
-                   }
+                    }
                 }
                 LOG(DEBUG) << "Autoconfiguration: done:\n" << conf;
             }
@@ -1559,19 +1573,19 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
     }
 
     if (new_radio_iface_name_update_req != 0 && !new_radio_iface_name.empty()) {
-	    // Extract the suffix from the original iface name
+        // Extract the suffix from the original iface name
 
-	    //update prplmesh config file for persistance
-	    const std::string package_name = "prplmesh";
-	    const std::string section_type = "wifi-device";
-	    const std::string option_name  = "hostap_iface";
+        //update prplmesh config file for persistance
+        const std::string package_name = "prplmesh";
+        const std::string section_type = "wifi-device";
+        const std::string option_name  = "hostap_iface";
 
-	    beerocks::bpl::cfg_update_prplmesh_option(package_name, section_type, option_name, base_radio_iface_name, new_radio_iface_name);
+        beerocks::bpl::cfg_update_prplmesh_option(package_name, section_type, option_name, base_radio_iface_name, new_radio_iface_name);
 
-            // Delete previous interface
-            if (!m_nl80211_client->remove_vap_interface(base_interface)) {
-	        LOG(ERROR) << "Failed to remove vap iface: " << base_interface;
-            } 
+        // Delete previous interface
+        if (!m_nl80211_client->remove_vap_interface(base_interface)) {
+            LOG(ERROR) << "Failed to remove vap iface: " << base_interface;
+        } 
     }
 
     if (abort) {
@@ -2270,6 +2284,36 @@ int ap_wlan_hal_nl80211::add_bss(std::string &ifname, son::wireless_utils::sBssI
     return fd;
 }
 
+bool ap_wlan_hal_nl80211::remove_mld_link(std::string &link_iface_name, sMacAddr &link_mac) {
+    int8_t link_id = -1;
+    std::string base_iface;
+    extractLinkBaseInterface(link_iface_name, base_iface);
+    std::string link_mac_string;
+    LOG(DEBUG) << "Base iface " << base_iface ;
+    link_mac_string = tlvf::mac_to_string(link_mac);
+    if (!m_nl80211_client->get_bss_link_id(base_iface, link_mac_string, link_id)) {
+        LOG(ERROR) << " get_bss_link_id failed " ;
+        return false;
+    }
+    LOG(DEBUG) << "link id " << link_id;
+    std::stringstream remove_cmd;
+    remove_cmd << "LINK_REMOVE -l " << static_cast<int>(link_id);
+    LOG(DEBUG) << "CMD framed : " << remove_cmd.str();
+    if (!wpa_ctrl_send_msg(remove_cmd.str(), base_iface)) {
+        LOG(ERROR) << "Failed to remove link: " << link_id << " of " <<base_iface;
+        return false;
+    }
+    // remove the fd for this link iface
+    base_wlan_hal_nl80211::remove_interface(link_iface_name);
+    base_wlan_hal_nl80211::remove_interface(base_iface);
+    return true;
+
+}
+
+bool ap_wlan_hal_nl80211::remove_bss(std::string &iface_name, sMacAddr &iface_mac) {
+	return true;
+}
+
 bool ap_wlan_hal_nl80211::add_mld_link(std::string &new_iface,std::string &prev_iface,son::wireless_utils::sBssInfoConf &bss_conf,
                                        std::string bridge)
 {
@@ -2621,8 +2665,14 @@ void ap_wlan_hal_nl80211::set_sta_enable(bool enable)
 
 bool ap_wlan_hal_nl80211::remove_affiliated_ap(const sMacAddr &bssid)
 {
-    LOG(TRACE) << __func__ << " - NOT IMPLEMENTED!";
-    return false;
+    std::list<son::wireless_utils::sBssInfoConf> bss_info_conf_list;
+    son::wireless_utils::sBssInfoConf bss_info_conf;
+    bss_info_conf.bssid = bssid;
+    bss_info_conf.teardown = true;
+    bss_info_conf_list.push_back(bss_info_conf);
+    /* framing for removing this bssid */
+    update_vap_credentials(bss_info_conf_list, "", "", "");
+    return true;
 }
 
 bool ap_wlan_hal_nl80211::get_mld_neighbor_bss_config(son::wireless_utils::sBssInfoConf &bss_config, int8_t mld_id)
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
index eb98ffd16..40f53bdff 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
@@ -140,6 +140,9 @@ public:
     prplmesh::hostapd::Configuration load_hostapd_config(const std::string &radio_iface_name, const std::string &bssid);
     virtual bool reload_all_ap_vaps() override;
 
+    bool remove_mld_link(std::string &link_iface_name, sMacAddr &link_mac);
+    bool remove_bss(std::string &iface_name, sMacAddr &iface_mac);
+
     bool add_mld_link(std::string &new_iface,std::string &prev_iface,son::wireless_utils::sBssInfoConf &bss_conf,
                                        std::string bridge);
 
diff --git a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
index 380c4da85..3f90b238d 100644
--- a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
@@ -1160,8 +1160,6 @@ bool base_wlan_hal_nl80211::register_wpa_ctrl_interface(const std::string &inter
 
     auto wpa_ctrl_path = m_hal_conf.wpa_ctrl_path;
 
-    size_t link_pos = 0;
-
     // Build wpa_ctrl path for secondary BSSs.
     if (interface.compare(get_iface_name()) != 0) {
 
@@ -1172,24 +1170,23 @@ bool base_wlan_hal_nl80211::register_wpa_ctrl_interface(const std::string &inter
             pos += 1;
         }
 
-        // Append the interface name to the path.
-        wpa_ctrl_path = wpa_ctrl_path.substr(0, pos) + interface;
-        if (beerocks::os_utils::wpa_ctrl_skt_exists(wpa_ctrl_path)) {
-            LOG(DEBUG) << " wpa_ctrl_path for interface " << interface << " exists in " << wpa_ctrl_path;
-        } else {
-            link_pos = interface.find("_link");
-            if (link_pos == std::string::npos) {
-                wpa_ctrl_path = wpa_ctrl_path.substr(0, pos) + interface + "/" + interface;
-            } else {
-                wpa_ctrl_path = wpa_ctrl_path.substr(0, pos) +  interface.substr(0, link_pos)  + "/" + interface;
-            }
+        int retry_count = 6; //count intialized to retry for 500 milliseconds max
+
+        do {
+            retry_count--;
+            wpa_ctrl_path = wpa_ctrl_path.substr(0, pos) + interface;
             if (beerocks::os_utils::wpa_ctrl_skt_exists(wpa_ctrl_path)) {
                 LOG(DEBUG) << " wpa_ctrl_path for interface " << interface << " exists in " << wpa_ctrl_path;
+                break;
             } else {
                 LOG(ERROR) << " wpa_ctrl_path for interface " << interface << " not exists in " << wpa_ctrl_path;
-                return false;
+                LOG(ERROR) << " Try one more time after 100 millosecond";
+                std::this_thread::sleep_for(std::chrono::milliseconds(100));
+                if (retry_count <= 0) {
+                    return false;
+                }
             }
-        }
+        }while(retry_count > 0);
     }
     LOG(DEBUG) << "Built wpa_ctrl_path " << wpa_ctrl_path << " for interface " << interface;
     if (!m_wpa_ctrl_client.add_interface(interface, wpa_ctrl_path, m_fds_ext_events)) {
-- 
2.34.1

