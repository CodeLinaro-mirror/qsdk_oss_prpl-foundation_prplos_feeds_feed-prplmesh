From 4fb7a3c75abca84ab87cad6e2db6d8695e6e1e5b Mon Sep 17 00:00:00 2001
From: Girija P <gpandiya@qti.qualcomm.com>
Date: Tue, 13 Jan 2026 14:21:35 +0530
Subject: [PATCH 38/54] MLO RELOAD_CONFIG_BSS change

Signed-off-by: Girija P <gpandiya@qti.qualcomm.com>
---
 .../beerocks/bwl/include/bwl/nl80211_client.h |  2 +
 .../bwl/include/bwl/nl80211_client_impl.h     |  1 +
 .../bwl/nl80211/ap_wlan_hal_nl80211.cpp       | 43 +++++++++----
 .../bwl/nl80211/base_wlan_hal_nl80211.cpp     | 10 ++-
 .../bwl/shared/nl80211_client_impl.cpp        | 64 +++++++++++++++++++
 5 files changed, 103 insertions(+), 17 deletions(-)

diff --git a/common/beerocks/bwl/include/bwl/nl80211_client.h b/common/beerocks/bwl/include/bwl/nl80211_client.h
index b70bdbc38..d6070df16 100644
--- a/common/beerocks/bwl/include/bwl/nl80211_client.h
+++ b/common/beerocks/bwl/include/bwl/nl80211_client.h
@@ -626,6 +626,8 @@ public:
     virtual bool get_radio_idx_from_link_id(const std::string &interface_name, int link_id, int &radio_idx) = 0;
 
     virtual bool remove_vap_interface(std::string &iface) =0;
+    
+    virtual bool get_mlo_active_link_count(std::string &bss,int8_t &active_link) =0;
 };
 
 } // namespace bwl
diff --git a/common/beerocks/bwl/include/bwl/nl80211_client_impl.h b/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
index 9a7a98aea..ff31487bc 100644
--- a/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
+++ b/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
@@ -200,6 +200,7 @@ public:
 
     virtual bool remove_vap_interface(std::string &iface) override;
 
+    virtual bool get_mlo_active_link_count(std::string &bss,int8_t &active_link) override;
 private:
     /**
      * NL80211 socket to send messages and receive responses to/from the WiFi driver.
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
index 275255eba..fac0bdd6a 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
@@ -1573,26 +1573,41 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                         } else {
                             if(links_present > 1)
                             {
-                                const std::string cmd("STOP_MLD");
-                                if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
-                                    LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
-                                    continue;
+                                LOG(DEBUG) << "CHECK FOR TRIGGER STOP_MLD " ;
+                                int8_t active_link = 0;
+                                m_nl80211_client->get_mlo_active_link_count(vap_info.bss,active_link);
+
+                                LOG(DEBUG) << "Active link count: " <<  active_link;
+
+                                if (active_link == links_present) {
+                                    LOG(DEBUG) << "TRIGGER STOP_MLD FOR " << vap_info.bss;
+                                    const std::string cmd("STOP_MLD");
+                                    if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
+                                        LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
+                                        continue;
+                                    }
+                                    LOG(INFO) << "STOP_MLD successful";
                                 }
-                                LOG(INFO) << "STOP_MLD successful";
-                                const std::string cmd_2("RELOAD_CONFIG");
-                                if (!wpa_ctrl_send_msg(cmd_2,vap_info.bss)) {
-                                    LOG(ERROR) << "Autoconfiguration: \"" << cmd_2 << "\" command to hostapd has failed";
+                                std::stringstream reload_bss;
+                                std::string conf_path =
+                                    std::string(bwl::nl80211::base_ctrl_path) + "hostapd-" + m_radio_info.phy_name + ".conf";
+                                reload_bss << "RELOAD_CONFIG_BSS bss_config=" <<conf_path;
+                                if (!wpa_ctrl_send_msg(reload_bss.str(),vap_info.bss)) {
+                                    LOG(ERROR) << reload_bss.str() << " command to hostapd has failed - send reload_config";
                                     continue;
                                 }
-                                LOG(INFO) << "RELOAD_CONFIG successful";
+                                LOG(INFO) << "RELOAD_CONFIG_BSS successful";
                             } else {
                                 const std::string cmd("STOP_AP");
-                                if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
-                                    LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
-                                    continue;
+                                if (m_radio_info.frequency_band == beerocks::eFreqType::FREQ_6G) {
+                                    LOG(DEBUG) << "SKIP STOP_AP ON 6G RADIO: " ;
+                                } else {
+                                    if (!wpa_ctrl_send_msg(cmd,vap_info.bss)) {
+                                        LOG(ERROR) << "Autoconfiguration: \"" << cmd << "\" command to hostapd has failed";
+                                        continue;
+                                    }
+                                    LOG(INFO) << "STOP_AP successful";
                                 }
-                                LOG(INFO) << "STOP_AP successful";
-
                                 std::stringstream reload_bss;
                                 std::string conf_path =
                                     std::string(bwl::nl80211::base_ctrl_path) + "hostapd-" + m_radio_info.phy_name + ".conf";
diff --git a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
index 4afac3a6d..cb2fe240c 100644
--- a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
@@ -1041,10 +1041,14 @@ bool base_wlan_hal_nl80211::send_nl80211_msg(uint8_t command, int flags,
     if (ifName.empty()) {
         iface_name = get_iface_name();
     }
+
+    std::string base_iface_name;
+    extractLinkBaseInterface(iface_name, base_iface_name);
+
     int iface_index;
-    if ((m_iface_index.find(iface_name) == m_iface_index.end()) ||
-        ((iface_index = m_iface_index.at(iface_name)) <= 0)) {
-        LOG(ERROR) << "Failed getting the index of interface " << iface_name;
+    if ((m_iface_index.find(base_iface_name) == m_iface_index.end()) ||
+        ((iface_index = m_iface_index.at(base_iface_name)) <= 0)) {
+        LOG(ERROR) << "Failed getting the index of interface " << base_iface_name;
 
         return false;
     }
diff --git a/common/beerocks/bwl/shared/nl80211_client_impl.cpp b/common/beerocks/bwl/shared/nl80211_client_impl.cpp
index 7b96a2ea9..797208928 100644
--- a/common/beerocks/bwl/shared/nl80211_client_impl.cpp
+++ b/common/beerocks/bwl/shared/nl80211_client_impl.cpp
@@ -1891,4 +1891,68 @@ bool nl80211_client_impl::get_radio_idx_from_link_id(const std::string &interfac
     return true;
 }
 
+bool nl80211_client_impl::get_mlo_active_link_count(std::string &interface_name,int8_t &link_count)
+{
+    std::string base_iface;
+    bwl::base_wlan_hal::extractLinkBaseInterface(interface_name, base_iface);
+
+    if (!m_socket) {
+        LOG(ERROR) << "Socket is NULL!";
+        return false;
+    }
+
+    link_count = 0;
+
+    // Get the interface index for given interface name
+    int iface_index = if_nametoindex(base_iface.c_str());
+    if (0 == iface_index) {
+        LOG(ERROR) << "Failed to read the index of interface " << base_iface << " : "
+                   << strerror(errno);
+
+        return false;
+    }
+
+    return m_socket.get()->send_receive_msg(
+        NL80211_CMD_GET_INTERFACE, 0,
+        [&](struct nl_msg *msg) -> bool {
+            nla_put_u32(msg, NL80211_ATTR_IFINDEX, iface_index);
+
+            return true;
+        },
+        [&](struct nl_msg *msg) {
+            struct nlattr *tb[NL80211_ATTR_MAX + 1];
+            struct genlmsghdr *gnlh = (struct genlmsghdr *)nlmsg_data(nlmsg_hdr(msg));
+
+            // Parse the netlink message
+            if (nla_parse(tb, NL80211_ATTR_MAX, genlmsg_attrdata(gnlh, 0), genlmsg_attrlen(gnlh, 0),
+                          NULL)) {
+                LOG(ERROR) << "Failed to parse netlink message!";
+                return false;
+            }
+            if (tb[NL80211_ATTR_MLO_LINKS]) {
+                LOG(DEBUG) << " MLO LINK PRESENT ";
+                int ret = 0;
+                struct nlattr *attrs, *link[NL80211_ATTR_MAX + 1];
+                nla_for_each_nested(attrs, tb[NL80211_ATTR_MLO_LINKS], ret) {
+                   nla_parse_nested(link, NL80211_ATTR_MAX, attrs, NULL);
+
+                   if (link[NL80211_ATTR_MLO_LINK_ID]) {
+                      int8_t link_id;
+                      link_id = nla_get_u8(link[NL80211_ATTR_MLO_LINK_ID]);
+                      if (link[NL80211_ATTR_WIPHY_FREQ]) {
+                          LOG(DEBUG) << " Link_id: "<< link_id << " is active";
+                          link_count++; 
+                      } else {
+                          LOG(DEBUG) << " Link_id: "<< link_id << " is not active";
+                      }
+                   }
+                }
+            } else {
+                LOG(DEBUG) << " MLO LINK NOT PRESENT ";
+            }
+            return true;
+        });
+        return true;
+}
+
 } // namespace bwl
-- 
2.34.1

