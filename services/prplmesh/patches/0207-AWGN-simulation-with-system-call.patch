From d5927154de91be101145dca0e940b89c0eb10d8b Mon Sep 17 00:00:00 2001
From: Sudharsan Eswaran <eswaran@qti.qualcomm.com>
Date: Fri, 5 Jun 2026 16:28:40 +0530
Subject: [PATCH] AWGN simulation with system call

Signed-off-by: Sudharsan Eswaran <eswaran@qti.qualcomm.com>
---
 .../ap_manager/ap_manager.cpp                 | 17 ++++
 agent/src/beerocks/slave/son_slave_thread.cpp | 59 ++++++++++++
 common/beerocks/bwl/include/bwl/ap_wlan_hal.h | 10 ++
 .../bwl/nl80211/ap_wlan_hal_nl80211.cpp       | 20 ++++
 .../bwl/nl80211/ap_wlan_hal_nl80211.h         |  1 +
 common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp   |  8 ++
 common/beerocks/bwl/whm/ap_wlan_hal_whm.h     |  1 +
 .../tlvf/beerocks_message_action.yaml         |  9 +-
 .../tlvf/beerocks_message_apmanager.yaml      | 11 +++
 .../tlvf/beerocks_message_control.yaml        | 15 +++
 controller/nbapi/odl/radio.odl.in             |  6 ++
 controller/nbapi/on_action.cpp                | 96 ++++++++++++++++++-
 controller/nbapi/on_action.h                  |  4 +
 13 files changed, 254 insertions(+), 3 deletions(-)

diff --git a/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp b/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
index 0e0c5fe26..000bd6a79 100644
--- a/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
+++ b/agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
@@ -1508,6 +1508,23 @@ void ApManager::handle_cmdu(ieee1905_1::CmduMessageRx &cmdu_rx)
         }
         break;
     }
+    case beerocks_message::ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST: {
+        LOG(INFO) << "ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST received";
+
+        bool success = ap_wlan_hal->simulate_awgn();
+        LOG(INFO) << "simulate_awgn() returned: " << (success ? "success" : "failure");
+
+        // Send response back to slave thread
+        auto response = message_com::create_vs_message<
+            beerocks_message::cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE>(cmdu_tx);
+        if (!response) {
+            LOG(ERROR) << "Failed to create cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE";
+            return;
+        }
+        response->success() = success ? 1 : 0;
+        send_cmdu(cmdu_tx);
+        break;
+    }
 
     case beerocks_message::ACTION_APMANAGER_HOSTAP_CANCEL_ACTIVE_CAC_REQUEST: {
         auto request =
diff --git a/agent/src/beerocks/slave/son_slave_thread.cpp b/agent/src/beerocks/slave/son_slave_thread.cpp
index e62a6333f..d7a558e2a 100644
--- a/agent/src/beerocks/slave/son_slave_thread.cpp
+++ b/agent/src/beerocks/slave/son_slave_thread.cpp
@@ -1781,6 +1781,34 @@ bool slave_thread::handle_cmdu_control_message(int fd,
         send_cmdu(radio_manager.monitor_fd, cmdu_tx);
         break;
     }
+    case beerocks_message::ACTION_CONTROL_AWGN_SIMULATE_REQUEST: {
+        LOG(INFO) << "Received ACTION_CONTROL_AWGN_SIMULATE_REQUEST";
+
+        auto request = beerocks_header->addClass<
+            beerocks_message::cACTION_CONTROL_AWGN_SIMULATE_REQUEST>();
+        if (!request) {
+            LOG(ERROR) << "addClass cACTION_CONTROL_AWGN_SIMULATE_REQUEST failed";
+            return false;
+        }
+
+        sMacAddr radio_mac = request->radio_mac();
+        LOG(INFO) << "AWGN simulation requested for radio: " << radio_mac;
+
+        // Forward to AP manager which will call ap_wlan_hal->simulate_awgn()
+        // The AP manager will respond with ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE,
+        // which is handled in handle_cmdu_ap_manager_message() below.
+        auto request_out = message_com::create_vs_message<
+            beerocks_message::cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST>(
+            cmdu_tx, beerocks_header->id());
+        if (!request_out) {
+            LOG(ERROR) << "Failed to create cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST";
+            return false;
+         }
+
+        LOG(INFO) << "Forwarding AWGN simulation request to AP manager via wpa_ctrl";
+        send_cmdu(radio_manager.ap_manager_fd, cmdu_tx);
+        break;
+    }
     default: {
         LOG(ERROR) << "Unknown CONTROL message, action_op: " << int(beerocks_header->action_op());
         return false;
@@ -3589,6 +3617,37 @@ bool slave_thread::handle_cmdu_ap_manager_message(const std::string &fronthaul_i
         m_backhaul_manager_client->send_cmdu(cmdu_tx);
         break;
     }
+    case beerocks_message::ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE: {
+        // Forward the result back to the controller as ACTION_CONTROL_AWGN_SIMULATE_RESPONSE.
+        auto response_in = beerocks_header->addClass<
+            beerocks_message::cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE>();
+        if (!response_in) {
+            LOG(ERROR) << "addClass cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE failed";
+            return false;
+        }
+
+        bool success = (response_in->success() == 1);
+        LOG(INFO) << "AWGN simulation via wpa_ctrl " << (success ? "succeeded" : "failed");
+
+        // Forward response to controller
+        auto response_out = message_com::create_vs_message<
+            beerocks_message::cACTION_CONTROL_AWGN_SIMULATE_RESPONSE>(cmdu_tx);
+        if (response_out) {
+            response_out->success() = response_in->success();
+            if (!success) {
+                const std::string error = "AWGN simulation failed via wpa_ctrl DCS sim";
+                strlcpy(response_out->error_message(), error.c_str(), 127);
+                response_out->error_message()[127] = '\0';
+                LOG(ERROR) << "AWGN simulation failed: " << error;
+            }
+            send_cmdu_to_controller(fronthaul_iface, cmdu_tx);
+            LOG(DEBUG) << "Sent AWGN_SIMULATE_RESPONSE to controller: success="
+                       << (int)response_out->success();
+        } else {
+            LOG(ERROR) << "Failed to create cACTION_CONTROL_AWGN_SIMULATE_RESPONSE";
+        }
+        break;
+    }
     case beerocks_message::ACTION_APMANAGER_HOSTAP_AWGN_DETECTED_NOTIFICATION: {
         auto notification_in = beerocks_header->addClass<
             beerocks_message::cACTION_APMANAGER_HOSTAP_AWGN_DETECTED_NOTIFICATION>();
diff --git a/common/beerocks/bwl/include/bwl/ap_wlan_hal.h b/common/beerocks/bwl/include/bwl/ap_wlan_hal.h
index 08b1c1bda..a6a4ccb54 100644
--- a/common/beerocks/bwl/include/bwl/ap_wlan_hal.h
+++ b/common/beerocks/bwl/include/bwl/ap_wlan_hal.h
@@ -315,6 +315,16 @@ public:
     virtual bool switch_channel(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                                 int csa_beacon_count = 5) = 0;
 
+    /**
+     * @brief Simulate AWGN (Additive White Gaussian Noise) interference on the radio.
+     *
+     * Sends the DCS_SIM command to hostapd via the wpa_ctrl socket to trigger
+     * an AWGN simulation event.
+     *
+     * @return true on success or false on error.
+     */
+    virtual bool simulate_awgn() = 0;
+
     /**
      * @brief cancel active cac if exsits.
      *
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
index 025d36bd0..eb6ef9b6a 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
@@ -2780,6 +2780,26 @@ bool ap_wlan_hal_nl80211::switch_channel(int chan, beerocks::eWiFiBandwidth bw,
     return true;
 }
 
+bool ap_wlan_hal_nl80211::simulate_awgn()
+{
+    // Send DCS command to hostapd via wpa_ctrl socket.
+    // This is the socket-based equivalent of: hostapd_cli -i <iface> dcs sim 4
+    // The command triggers an AWGN simulation event in hostapd's DCS module.
+    //
+    // wpa_ctrl_send_msg() with no ifname argument sends to the radio/MainBSS interface,
+    // which is the same target as "hostapd_cli -i <iface>".
+    const std::string cmd = "DCS sim 4";
+    LOG(INFO) << "simulate_awgn: sending '" << cmd << "' via wpa_ctrl socket";
+
+    if (!wpa_ctrl_send_msg(cmd)) {
+        LOG(ERROR) << "simulate_awgn: wpa_ctrl_send_msg('" << cmd << "') failed!";
+        return false;
+    }
+
+    LOG(INFO) << "simulate_awgn: DCS sim 4 command sent successfully";
+    return true;
+}
+
 bool ap_wlan_hal_nl80211::cancel_cac(int chan, beerocks::eWiFiBandwidth bw,
                                      int vht_center_frequency, int secondary_chan)
 {
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
index cfd284e10..b1dd113e3 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.h
@@ -68,6 +68,7 @@ public:
                                       const std::string &client_mac) override;
     virtual bool switch_channel(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                                 int csa_beacon_count) override;
+    virtual bool simulate_awgn() override;
     virtual bool cancel_cac(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                             int secondary_chan) override;
     virtual bool failsafe_channel_set(int chan, int bw, int vht_center_frequency) override;
diff --git a/common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp b/common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp
index c6b9da5c8..8d6cfd165 100644
--- a/common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp
+++ b/common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp
@@ -874,6 +874,14 @@ bool ap_wlan_hal_whm::switch_channel(int chan, beerocks::eWiFiBandwidth bw,
     return status;
 }
 
+bool ap_wlan_hal_whm::simulate_awgn()
+{
+    // WHM platform does not use wpa_ctrl sockets; AWGN simulation via DCS_SIM
+    // is not supported on this platform. Return false to indicate unsupported.
+    LOG(WARNING) << "simulate_awgn: DCS_SIM not supported on WHM platform";
+    return false;
+}
+
 bool ap_wlan_hal_whm::cancel_cac(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                                  int secondary_chan)
 {
diff --git a/common/beerocks/bwl/whm/ap_wlan_hal_whm.h b/common/beerocks/bwl/whm/ap_wlan_hal_whm.h
index b3a5adb1a..45fe2c145 100644
--- a/common/beerocks/bwl/whm/ap_wlan_hal_whm.h
+++ b/common/beerocks/bwl/whm/ap_wlan_hal_whm.h
@@ -71,6 +71,7 @@ public:
                                       const std::string &client_mac) override;
     virtual bool switch_channel(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                                 int csa_beacon_count) override;
+    virtual bool simulate_awgn() override;
     virtual bool cancel_cac(int chan, beerocks::eWiFiBandwidth bw, int vht_center_frequency,
                             int secondary_chan) override;
     virtual bool failsafe_channel_set(int chan, int bw, int vht_center_frequency) override;
diff --git a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_action.yaml b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_action.yaml
index 5767393ae..091c31f7b 100755
--- a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_action.yaml
+++ b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_action.yaml
@@ -91,7 +91,10 @@ eActionOp_CONTROL:
   ACTION_CONTROL_CHANNEL_SCAN_ABORT_NOTIFICATION: 146
   ACTION_CONTROL_CHANNEL_SCAN_FINISHED_NOTIFICATION: 147
 
-  ACTION_CONTROL_ENUM_END: 148
+  ACTION_CONTROL_AWGN_SIMULATE_REQUEST: 148
+  ACTION_CONTROL_AWGN_SIMULATE_RESPONSE: 149
+
+  ACTION_CONTROL_ENUM_END: 150
 
 ####################################################
 ####################################################
@@ -276,8 +279,10 @@ eActionOp_APMANAGER:
   ACTION_APMANAGER_SET_FH_VAPS_STATE: 81
   ACTION_APMANAGER_UPDATE_HIDDEN_SSID_REQUEST: 82
   ACTION_APMANAGER_HOSTAP_AWGN_DETECTED_NOTIFICATION: 83
+  ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST: 84
+  ACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE: 85
 
-  ACTION_APMANAGER_ENUM_END: 84
+  ACTION_APMANAGER_ENUM_END: 86
 
 ####################################################
 ####################################################
diff --git a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_apmanager.yaml b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_apmanager.yaml
index f14308779..083b55186 100755
--- a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_apmanager.yaml
+++ b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_apmanager.yaml
@@ -387,3 +387,14 @@ cACTION_APMANAGER_UPDATE_HIDDEN_SSID_REQUEST:
 cACTION_APMANAGER_HOSTAP_AWGN_DETECTED_NOTIFICATION:
   _type: class
   params: sAwgnDetectedParams
+
+#################################################
+# AWGN SIMULATION (via wpa_ctrl DCS_SIM command)
+#################################################
+
+cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_REQUEST:
+  _type: class
+
+cACTION_APMANAGER_HOSTAP_AWGN_SIMULATE_RESPONSE:
+  _type: class
+  success: uint8_t  # 1=success, 0=failure
diff --git a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_control.yaml b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_control.yaml
index c2a9bdadb..8ba04e0e8 100755
--- a/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_control.yaml
+++ b/common/beerocks/tlvf/yaml/beerocks/tlvf/beerocks_message_control.yaml
@@ -298,3 +298,18 @@ cACTION_CONTROL_CHANNEL_SCAN_ABORT_NOTIFICATION:
 cACTION_CONTROL_CHANNEL_SCAN_FINISHED_NOTIFICATION:
   _type: class
   radio_mac: sMacAddr
+
+#################################################
+# AWGN SIMULATION
+#################################################
+
+cACTION_CONTROL_AWGN_SIMULATE_REQUEST:
+  _type: class
+  radio_mac: sMacAddr  # Radio MAC to target for AWGN simulation
+
+cACTION_CONTROL_AWGN_SIMULATE_RESPONSE:
+  _type: class
+  success: uint8_t  # 1=success, 0=failure
+  error_message:
+    _type: char
+    _length: [128]  # Error message if failed
diff --git a/controller/nbapi/odl/radio.odl.in b/controller/nbapi/odl/radio.odl.in
index 389f0ce5d..39de7eda4 100644
--- a/controller/nbapi/odl/radio.odl.in
+++ b/controller/nbapi/odl/radio.odl.in
@@ -176,6 +176,12 @@
             %in string channels_list
         );
 
+        /* Trigger AWGN simulation on a 6 GHz radio.
+        * This function sends a command to the agent to execute AWGN simulation
+        * using hostapd_cli. The radio must be operating on the 6 GHz band.
+        * The simulation uses the hardcoded command: hostapd_cli -i <iface> -l 0 dcs sim 4 */
+        %instance bool SimulateAWGN6GHz();
+
         %read-only object ScanResult [] {
             counted with ScanResultNumberOfEntries;
         }
diff --git a/controller/nbapi/on_action.cpp b/controller/nbapi/on_action.cpp
index d9cfae3b4..5d347ca98 100644
--- a/controller/nbapi/on_action.cpp
+++ b/controller/nbapi/on_action.cpp
@@ -1788,6 +1788,98 @@ amxd_status_t send_beacon_metrics_query(amxd_object_t *object, amxd_function_t *
     return amxd_status_ok;
 }
 
+/**
+ * @brief Simulate AWGN (Additive White Gaussian Noise) on 6 GHz radio
+ *
+ * This function triggers AWGN simulation on the specified radio if it's 6 GHz.
+ * It sends a vendor-specific message to the colocated Agent which executes:
+ * hostapd_cli -i wlan3 -l 0 dcs sim 4
+ *
+ * Example usage:
+ * ubus call Device.WiFi.DataElements.Network.Device.1.Radio.1 SimulateAWGN
+ *
+ * @param object The data model object (Radio instance)
+ * @param func The function being called
+ * @param args Function arguments (none required)
+ * @param ret Return value
+ * @return amxd_status_ok on success, error code otherwise
+ */
+amxd_status_t simulate_awgn_6ghz(amxd_object_t *object, amxd_function_t *func,
+                                 amxc_var_t *args, amxc_var_t *ret)
+{
+    auto controller_ctx = g_database->get_controller_ctx();
+
+    if (!controller_ctx) {
+        LOG(ERROR) << "SimulateAWGN: Failed to get controller context";
+        return amxd_status_unknown_error;
+    }
+
+    // Get the radio ID from the object path
+    amxc_var_t value;
+    amxc_var_init(&value);
+    amxd_object_get_param(object, "ID", &value);
+    std::string radio_mac_str = amxc_var_constcast(cstring_t, &value);
+    amxc_var_clean(&value);
+
+    if (radio_mac_str.empty()) {
+        LOG(ERROR) << "SimulateAWGN: radio MAC is empty";
+        return amxd_status_parameter_not_found;
+    }
+
+    sMacAddr radio_mac = tlvf::mac_from_string(radio_mac_str);
+
+    LOG(INFO) << "SimulateAWGN: Request for radio: " << radio_mac_str;
+
+    // Get agent that owns this radio
+    auto agent = g_database->get_agent_by_radio_uid(radio_mac);
+    if (!agent) {
+        LOG(ERROR) << "SimulateAWGN: Agent not found for radio: " << radio_mac_str;
+        return amxd_status_object_not_found;
+    }
+
+    // Get radio to check if it's 6 GHz
+    auto radio = agent->radios.get(radio_mac);
+    if (!radio) {
+        LOG(ERROR) << "SimulateAWGN: Radio not found: " << radio_mac_str;
+        return amxd_status_object_not_found;
+    }
+
+    // Verify radio is 6 GHz band
+    if (!radio->supports_6ghz || radio->band != beerocks::FREQ_6G) {
+        LOG(ERROR) << "SimulateAWGN: Radio is not 6 GHz band: " << radio_mac_str;
+        return amxd_status_invalid_value;
+    }
+
+    LOG(INFO) << "SimulateAWGN: Verified radio is 6 GHz: " << radio_mac_str;
+
+    // Create CMDU message
+    uint8_t tx_buffer[beerocks::message::MESSAGE_BUFFER_LENGTH];
+    ieee1905_1::CmduMessageTx cmdu_tx(tx_buffer, sizeof(tx_buffer));
+
+    // Create AWGN simulate request
+    auto request = message_com::create_vs_message<
+        beerocks_message::cACTION_CONTROL_AWGN_SIMULATE_REQUEST>(cmdu_tx);
+
+    if (!request) {
+        LOG(ERROR) << "SimulateAWGN: Failed to create AWGN_SIMULATE_REQUEST";
+        return amxd_status_unknown_error;
+    }
+
+    // Set radio MAC
+    request->radio_mac() = radio_mac;
+
+    // Send to agent using the helper function
+    if (!son_actions::send_cmdu_to_agent(agent->al_mac, cmdu_tx, *g_database, radio_mac_str)) {
+        LOG(ERROR) << "SimulateAWGN: Failed to send message to agent: " << agent->al_mac;
+        return amxd_status_unknown_error;
+    }
+
+    LOG(INFO) << "SimulateAWGN: Sent AWGN simulation request to agent: " << agent->al_mac
+              << " for radio: " << radio_mac_str;
+
+    return amxd_status_ok;
+}
+
 std::vector<beerocks::nbapi::sActionsCallback> get_actions_callback_list(void)
 {
     const std::vector<beerocks::nbapi::sActionsCallback> actions_list = {
@@ -1853,7 +1945,9 @@ std::vector<beerocks::nbapi::sFunctions> get_func_list(void)
         {"send_policy_config_request", DATAELEMENTS_ROOT_DM ".Network.Device.SendPolicyConfigRequest",
          send_policy_config_request},
         {"send_beacon_metrics_query", DATAELEMENTS_ROOT_DM ".Network.Device.SendBeaconMetricsQuery",
-         send_beacon_metrics_query}};
+         send_beacon_metrics_query},
+        {"simulate_awgn_6ghz", DATAELEMENTS_ROOT_DM ".Network.Device.Radio.SimulateAWGN6GHz",
+         simulate_awgn_6ghz}};
     return functions_list;
 }
 
diff --git a/controller/nbapi/on_action.h b/controller/nbapi/on_action.h
index 3897fc398..9809d01f8 100644
--- a/controller/nbapi/on_action.h
+++ b/controller/nbapi/on_action.h
@@ -23,6 +23,10 @@ std::vector<beerocks::nbapi::sEvents> get_events_list(void);
 std::vector<beerocks::nbapi::sFunctions> get_func_list(void);
 beerocks::nbapi::ambiorix_func_ptr get_access_point_commit(void);
 
+// AWGN Simulation function
+amxd_status_t simulate_awgn_6ghz(amxd_object_t *object, amxd_function_t *func,
+                                 amxc_var_t *args, amxc_var_t *ret);
+
 extern son::db *g_database;
 
 /**
-- 
2.34.1

