From 3f1b327eba7d3b12851bb7763081d93d8a1a7e87 Mon Sep 17 00:00:00 2001
From: Girija P <gpandiya@qti.qualcomm.com>
Date: Thu, 8 Jan 2026 00:29:39 +0530
Subject: [PATCH 30/31] MLO related fixes on top of Jan5 baseline

Signed-off-by: Girija P <gpandiya@qti.qualcomm.com>
---
 .../fronthaul_manager/monitor/monitor.cpp     |  2 +-
 agent/src/beerocks/slave/son_slave_thread.cpp |  2 +
 .../bcl/include/bcl/son/son_wireless_utils.h  |  1 +
 .../bwl/nl80211/ap_wlan_hal_nl80211.cpp       | 25 +++++++-----
 .../beerocks/master/beerocks_master_main.cpp  | 40 +++++++++++++++++++
 controller/src/beerocks/master/controller.cpp |  1 +
 .../platform/bpl/cfg/uci/bpl_cfg_wifi.cpp     | 36 ++++++++++++-----
 7 files changed, 87 insertions(+), 20 deletions(-)

diff --git a/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp b/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
index a005411dc..445a2a972 100644
--- a/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
+++ b/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
@@ -1140,7 +1140,7 @@ bool Monitor::update_ap_stats()
                 default: break;
             }
  
-            char hostapd_iface[BPL_IFNAME_LEN] = "";
+            std::string hostapd_iface;
             if (beerocks::bpl::cfg_get_hostap_iface(radio_id,hostapd_iface) < 0) {
                 LOG(ERROR) << " Failed to get latest hostapd iface" ;
                 return false;
diff --git a/agent/src/beerocks/slave/son_slave_thread.cpp b/agent/src/beerocks/slave/son_slave_thread.cpp
index 835e25cbb..5f0211142 100644
--- a/agent/src/beerocks/slave/son_slave_thread.cpp
+++ b/agent/src/beerocks/slave/son_slave_thread.cpp
@@ -5772,6 +5772,8 @@ bool slave_thread::add_agent_ap_mld_configuration_tlv(ieee1905_1::CmduMessageTx
                 return false;
             }
         }
+    } else {
+        LOG(DEBUG) << "Debug: AP MLD configurations empty in master db";
     }
 
     return true;
diff --git a/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h b/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
index d6fb40cb3..f40f3dd77 100644
--- a/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
+++ b/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
@@ -24,6 +24,7 @@
 #include <set>
 #include <string>
 #include <vector>
+#include <stdexcept>
 
 constexpr float NOISE_FIGURE = 8.0f;
 
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
index a301f404a..fdecc6e20 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
@@ -128,7 +128,7 @@ int fetch_mld_id_pair(int mld_id, std::string &mld_grp_primary_ifname) {
                std::istringstream iss(line);
                std::string id_str, name_str;
                if (getline(iss, id_str, ',') && getline(iss, name_str)) {
-                       if (std::stoi(id_str) == mld_id) {
+                       if (beerocks::string_utils::stoi(id_str) == mld_id) {
                                name = name_str;
                                break;
                        }
@@ -294,8 +294,8 @@ prplmesh::hostapd::Configuration ap_wlan_hal_nl80211::load_hostapd_config(const
             int chan_start = -1, chan_end = -1;
             size_t dash_pos = chanlist_str.find('-');
             if (dash_pos != std::string::npos) {
-                chan_start = std::stoi(chanlist_str.substr(0, dash_pos));
-                chan_end = std::stoi(chanlist_str.substr(dash_pos + 1));
+                chan_start = beerocks::string_utils::stoi(chanlist_str.substr(0, dash_pos));
+                chan_end = beerocks::string_utils::stoi(chanlist_str.substr(dash_pos + 1));
             } else {
                 LOG(ERROR) << "Unexpected chanlist format in " << try_fname << ": " << chanlist_str;
                 continue;
@@ -1226,6 +1226,11 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             bss_it.backhaul_wps_passphrase = backhaul_wps_passphrase;
         }
         bss_name_changed = false;
+        int mld_id_val = -1;
+        if (!bss_it.mld_id.empty()) {
+            mld_id_val = beerocks::string_utils::stoi(bss_it.mld_id); // Convert string to int
+        }
+
         if (conf_bssid == beerocks::net::network_utils::WILD_MAC_STRING) {          
             LOG(INFO) << "create new vap for wildcard bssid ";
 
@@ -1236,8 +1241,9 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             ap_wlan_hal_nl80211::generate_new_bssid(new_vap_index,new_bssid);
 
             LOG(INFO) << "NEW VAP Ifname: "<< new_vap_name << " Index: " << new_vap_index <<  " BSSID: " << new_bssid;
-            if (bss_it.mld_id >= 0) {
-                std::string mld_grp_bss_name = get_ifname_wrt_mld_grp(bss_it.mld_id, new_vap_name);
+
+            if (mld_id_val >= 0) {
+                std::string mld_grp_bss_name = get_ifname_wrt_mld_grp(mld_id_val, new_vap_name);
                 if (mld_grp_bss_name == new_vap_name) {
                     LOG(ERROR) << new_vap_name << ": no need to change bss name for this mld grouping";
                 } else {
@@ -1247,6 +1253,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
                 }
             }
 
+
             //to update uci with whatever update name we get
             bss_it.old_iface_name = new_vap_name;
 
@@ -1260,7 +1267,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
             bss_it.bssid = new_bssid;
             std::string bridge = "br-lan";
 
-            if ((bss_it.mld_id >=0) && bss_name_changed) {
+            if ((mld_id_val >=0) && bss_name_changed) {
                 std::string prev_iface = "";
                 if (!add_mld_link(new_vap_name,prev_iface,bss_it,bridge)) {
                     LOG(ERROR) << " add_mld_link failed for " << new_vap_name ;
@@ -1424,8 +1431,8 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
 
             conf.set_create_vap_value(vap_id, "wds_sta", "1");
 
-            if (bss_it.mld_id >= 0) {
-                std::string new_bss_name = get_ifname_wrt_mld_grp(bss_it.mld_id, bss);
+            if (mld_id_val >= 0) {
+                std::string new_bss_name = get_ifname_wrt_mld_grp(mld_id_val, bss);
                 if (new_bss_name == bss) {
                     LOG(INFO) << bss << ": No need to change bss name for this mld grouping";
                 } else {
@@ -1516,7 +1523,7 @@ bool ap_wlan_hal_nl80211::update_vap_credentials(
 
                             if (valid) {
                                 LOG(INFO) << "num_links found";
-                                links_present = std::stoi(num_links_str);
+                                links_present = beerocks::string_utils::stoi(num_links_str);
                             } else {
                                 LOG(WARNING) << "num_links value is not a valid number: '" << num_links_str << "'";
                             }
diff --git a/controller/src/beerocks/master/beerocks_master_main.cpp b/controller/src/beerocks/master/beerocks_master_main.cpp
index e1f736777..9360e810d 100644
--- a/controller/src/beerocks/master/beerocks_master_main.cpp
+++ b/controller/src/beerocks/master/beerocks_master_main.cpp
@@ -778,6 +778,46 @@ int main(int argc, char *argv[])
         if (beerocks::bpl::bpl_cfg_get_wireless_settings(wireless_settings)) {
             for (const auto &configuration : wireless_settings) {
                 master_db.add_bss_info_configuration(configuration);
+    /* Debug start */
+                if (!configuration.mld_id.empty()) {
+                    LOG(DEBUG) << "Debug: mld_id not empty: " << configuration.mld_id;
+                } else {
+                    LOG(DEBUG) << "Debug: mld_id empty";
+                }
+
+
+    /* Debug end */
+                /* Logic to add mld_info configuration */
+				int mld_unit = -1;
+                if (!configuration.mld_id.empty()) {
+					size_t pos = 0;
+					int parsed = std::stoi(configuration.mld_id, &pos, 10);
+					if (pos == configuration.mld_id.size()) {
+						mld_unit = parsed;  // parsed successfully
+                        LOG(DEBUG) << "Debug: mld_unit: " << mld_unit;
+					} else {
+						mld_unit = -1;
+						LOG(WARNING) << "Trailing characters in mld_id '"
+							<< configuration.mld_id << "'; keeping mld_unit="
+							<< mld_unit << " (disabled)";
+					}
+
+					if (mld_unit >= 0) {
+						son::wireless_utils::sMldInfoConf mld_info;
+						mld_info.ssid = configuration.ssid;
+
+						// TODO: Read Modes from Configuration (PPM-3588)
+						mld_info.str   = true;
+						mld_info.nstr  = false;
+						mld_info.emlsr = true;
+						mld_info.emlmr = false;
+
+						master_db.add_mld_info_configuration(mld_info, std::to_string(mld_unit));
+                        LOG(DEBUG) << "Debug: add_mld_info_configuration call completed";
+
+					}
+
+                }
             }
         } else {
             LOG(DEBUG) << "failed to read wireless settings";
diff --git a/controller/src/beerocks/master/controller.cpp b/controller/src/beerocks/master/controller.cpp
index e515f5122..2943ef675 100644
--- a/controller/src/beerocks/master/controller.cpp
+++ b/controller/src/beerocks/master/controller.cpp
@@ -1697,6 +1697,7 @@ bool Controller::handle_cmdu_1905_autoconfiguration_WSC(const sMacAddr &src_mac,
     }
 
     if (agent->max_num_mlds != 0 && radio->eht_supported) {
+        LOG(DEBUG) << "Debug: Trying to add wifi7 tlvs";
         if (!add_backhaul_sta_mld_configuration_tlv(database, cmdu_tx, *agent)) {
             LOG(ERROR) << "Couldn't add Backhaul STA MLD Configuration TLV";
         }
diff --git a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
index 5c9e1b64d..84aa01836 100644
--- a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
+++ b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
@@ -87,7 +87,7 @@ static bool bpl_cfg_get_bss_configuration(const std::string &section_name,
 	if (mld_index_str.empty()) {
 	    return -1;
 	}
-	int mld_index = std::stoi(mld_index_str);
+	int mld_index = beerocks::string_utils::stoi(mld_index_str);
 	MAPF_DBG("Section_name:" << section_name << " mld_index: " << mld_index);
 	return mld_index;
     };
@@ -95,7 +95,7 @@ static bool bpl_cfg_get_bss_configuration(const std::string &section_name,
     if (options.find("mld") == options.end()) {
 	    MAPF_DBG("Section_name: " << section_name << " - No MLD option in this bss conf section");
     } else {
-	    configuration.mld_id = get_mld_id(options["mld"]);
+	    configuration.mld_id = std::to_string(get_mld_id(options["mld"]));
     }
 
     return true;
@@ -408,8 +408,16 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
     const std::string section_type = "wifi-iface";
     const std::string option_name  = "ifname";
     std::string section_name;
-    
-    if(configuration.mld_id ==-1){
+
+    int mld_id_val = -1;
+    if (!configuration.mld_id.empty() &&
+            configuration.mld_id != std::to_string(DISABLED_MLDUNIT)) {
+
+        mld_id_val = beerocks::string_utils::stoi(configuration.mld_id); // Convert string to int
+    }
+
+
+    if(mld_id_val ==-1){
 	    if (!uci_find_section_by_option(package_name, section_type, option_name, iface, section_name)) {
 		    LOG(ERROR) << "Failed to find configuration section for interface " << iface;
 		    return false;
@@ -515,7 +523,7 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
     }
 
     //FOR MLD configuration update
-    if(configuration.mld_id>=0)
+    if(mld_id_val>=0)
     {
 
         //lambda to fill encription
@@ -564,6 +572,14 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
             -> OptionsUnorderedMap {
             OptionsUnorderedMap options;
 
+            int conf_mld_id_val = -1;
+            if (!config.mld_id.empty() &&
+                    config.mld_id != std::to_string(DISABLED_MLDUNIT)) {
+
+                conf_mld_id_val = beerocks::string_utils::stoi(config.mld_id); // Convert string to int
+            }
+
+
             options["ssid"] = config.ssid;
             options["encryption"] = get_encryption(config.authentication_type, config.encryption_type);
             
@@ -582,7 +598,7 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
             }
 
             options["mode"] = "ap";
-            options["mld"] = "mld" + std::to_string(config.mld_id + 1);
+            options["mld"] = "mld" + std::to_string(conf_mld_id_val + 1);
             options["network"] = "lan";
 
             if (!config.multi_ap.empty()) {
@@ -604,10 +620,10 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
 
         //Creating a new wifi-mld section if it doesnt exists
         const std::string mld_section_type = "wifi-mld";
-        std::string mld_section_name = "mld" + std::to_string(configuration.mld_id+1);
+        std::string mld_section_name = "mld" + std::to_string(mld_id_val+1);
 
         if(!uci_section_exists(package_name, mld_section_type, mld_section_name)) {
-            LOG(INFO) << "The mld section doesnt exist for mld" << configuration.mld_id;
+            LOG(INFO) << "The mld section doesnt exist for mld" << mld_id_val;
 
             if(!uci_add_section(package_name, mld_section_type, mld_section_name, true)) {
                     LOG(ERROR) << "Failed to add new section for MLD: " << mld_section_name;
@@ -640,7 +656,7 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
         }
 
         if (!uci_set_section(package_name, mld_section_type, mld_section_name, mldOptions, true)) {
-            LOG(ERROR) << "Failed to set wireless configuration for MLD: " << configuration.mld_id << " at section " << mld_section_name;
+            LOG(ERROR) << "Failed to set wireless configuration for MLD: " << mld_id_val << " at section " << mld_section_name;
             return false;
         }
 
@@ -680,7 +696,7 @@ bool bpl_cfg_set_wifi_credentials(const std::string &iface,
         }
 
         std::string device_section = resolve_device_section(configuration.freq_band);
-        std::string MLD_ID = "mld" + std::to_string(configuration.mld_id + 1);
+        std::string MLD_ID = "mld" + std::to_string(mld_id_val + 1);
 
         std::vector<std::string> all_iface_sections;
         if (!uci_get_all_sections(package_name, section_type, all_iface_sections)) {
-- 
2.34.1

