From c09be6103656be0ae610186221f7536d7c3c4a62 Mon Sep 17 00:00:00 2001
From: Nirmal Raj S <nirm@qti.qualcomm.com>
Date: Wed, 14 Jan 2026 13:49:02 +0530
Subject: [PATCH] enabling-quad-band-support

Signed-off-by: Nirmal Raj S <nirm@qti.qualcomm.com>
---
 .../fronthaul_manager/monitor/monitor.cpp     |  2 +
 agent/src/beerocks/slave/agent_db.h           |  3 +
 .../beerocks/slave/beerocks_agent_main.cpp    | 10 +++
 agent/src/beerocks/slave/son_slave_thread.cpp | 15 +++-
 .../slave/tasks/ap_autoconfiguration_task.cpp | 22 +++++-
 agent/src/beerocks/slave/tlvf_utils.cpp       |  3 +
 .../bcl/include/bcl/beerocks_defines.h        |  4 +-
 .../bcl/include/bcl/son/son_wireless_utils.h  |  2 +-
 common/beerocks/bcl/source/beerocks_utils.cpp |  4 +
 .../bcl/source/beerocks_wifi_channel.cpp      | 10 ++-
 .../bcl/source/son/son_wireless_utils.cpp     | 24 +++++-
 .../beerocks/bwl/include/bwl/nl80211_client.h |  2 +
 .../bwl/include/bwl/nl80211_client_impl.h     |  2 +
 .../bwl/nl80211/ap_wlan_hal_nl80211.cpp       | 36 ++++++++-
 .../bwl/nl80211/base_wlan_hal_nl80211.cpp     |  2 +-
 .../bwl/shared/nl80211_client_impl.cpp        | 79 +++++++++++++++++++
 common/beerocks/scripts/prplmesh_utils.sh.in  |  3 +-
 controller/src/beerocks/bml/bml_defs.h        |  2 +-
 .../platform/bpl/cfg/uci/bpl_cfg_wifi.cpp     | 17 +++-
 19 files changed, 222 insertions(+), 20 deletions(-)

diff --git a/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp b/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
index 7bf2c9668..3b057aeef 100644
--- a/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
+++ b/agent/src/beerocks/fronthaul_manager/monitor/monitor.cpp
@@ -1138,6 +1138,8 @@ bool Monitor::update_ap_stats()
                 case beerocks::FREQ_24G: radio_id = 0; break;
                 case beerocks::FREQ_5G: radio_id = 1;  break;
                 case beerocks::FREQ_6G: radio_id = 2;  break;
+                case beerocks::FREQ_5GL: radio_id = 1;  break;
+                case beerocks::FREQ_5GH: radio_id = 3;  break;
                 default: break;
             }
  
diff --git a/agent/src/beerocks/slave/agent_db.h b/agent/src/beerocks/slave/agent_db.h
index 1d9167e31..03bc7b5ba 100644
--- a/agent/src/beerocks/slave/agent_db.h
+++ b/agent/src/beerocks/slave/agent_db.h
@@ -180,6 +180,7 @@ public:
         uint8_t ap_maximum_links     = 0;
         uint8_t bsta_maximum_links   = 0;
         bool enable_auto_chansel_handling;
+        int8_t num_of_bands = 0;
     } device_conf;
 
     struct sControllerInfo {
@@ -422,6 +423,8 @@ public:
         std::shared_ptr<sSwitchChannelRequest> last_switch_channel_request;
 
         std::string chipset_vendor;
+
+        beerocks::eFreqType frequency_band = beerocks::eFreqType::FREQ_UNKNOWN;
     };
     struct {
         uint16_t max_number_of_vlans_ids;
diff --git a/agent/src/beerocks/slave/beerocks_agent_main.cpp b/agent/src/beerocks/slave/beerocks_agent_main.cpp
index 891a2bb33..39d3e2e09 100644
--- a/agent/src/beerocks/slave/beerocks_agent_main.cpp
+++ b/agent/src/beerocks/slave/beerocks_agent_main.cpp
@@ -287,10 +287,20 @@ static void fill_son_slave_config(const beerocks::config_file::sConfigSlave &bee
                     : 0;
     }
 
+        int8_t num_of_bands = 0;
+#ifdef USE_PRPLMESH_NLHAL
+        if (!nl80211_client->get_radio_band_info(num_of_bands)) {
+            LOG(ERROR) << "Failed to get number of band ";
+        }
+#endif
+
+    LOG(DEBUG) << "NUM_OF_BAND Updated in get_radio_band_info= " << num_of_bands;
+
     auto db = beerocks::AgentDB::get();
 
     db->device_conf.operating_system = beerocks::os_utils::get_os_name();
     db->em_handle_third_party        = beerocks_slave_conf.em_handle_third_party;
+    db->device_conf.num_of_bands = num_of_bands;
     //Get the Serial number of the device and current running software version
     //using Ambiorix dm.
     beerocks::bpl::get_software_version(db->device_conf.software_version);
diff --git a/agent/src/beerocks/slave/son_slave_thread.cpp b/agent/src/beerocks/slave/son_slave_thread.cpp
index 3e5197cf1..bf604f30d 100644
--- a/agent/src/beerocks/slave/son_slave_thread.cpp
+++ b/agent/src/beerocks/slave/son_slave_thread.cpp
@@ -2704,6 +2704,8 @@ bool slave_thread::handle_cmdu_ap_manager_message(const std::string &fronthaul_i
                        << ", Max bSTA MLD Links " << db->device_conf.bsta_maximum_links;
         }
 
+        radio->frequency_band = notification->params().frequency_band;
+
         save_channel_params_to_db(fronthaul_iface, notification->cs_params());
         if (notification->params().frequency_band != radio->wifi_channel.get_freq_type()) {
             LOG(ERROR) << "Radio wifi channel's frequncy types does not match the frequency type "
@@ -5726,9 +5728,16 @@ void slave_thread::save_channel_params_to_db(const std::string &fronthaul_iface,
 
     radio->tx_power_dB = params.tx_power;
 
-    radio->wifi_channel =
-        beerocks::WifiChannel(params.channel, params.vht_center_frequency,
-                              static_cast<beerocks::eWiFiBandwidth>(params.bandwidth));
+    // [NLHAL] For 5GL and 5GH Create WifiChannel instance based on freq_band instead of vht_center_frequency
+    if ((radio->frequency_band == beerocks::FREQ_5GL) || (radio->frequency_band == beerocks::FREQ_5GH)) {
+        radio->wifi_channel =
+            beerocks::WifiChannel(params.channel,radio->frequency_band,
+                                  static_cast<beerocks::eWiFiBandwidth>(params.bandwidth));
+    } else {
+        radio->wifi_channel =
+            beerocks::WifiChannel(params.channel, params.vht_center_frequency,
+                                  static_cast<beerocks::eWiFiBandwidth>(params.bandwidth));
+    }
 
     if (params.channel_ext_above_primary != radio->wifi_channel.get_ext_above_primary()) {
         LOG(ERROR) << "the channel_ext_above_primary" << params.channel_ext_above_primary
diff --git a/agent/src/beerocks/slave/tasks/ap_autoconfiguration_task.cpp b/agent/src/beerocks/slave/tasks/ap_autoconfiguration_task.cpp
index 56ed88b3a..76cbb6880 100644
--- a/agent/src/beerocks/slave/tasks/ap_autoconfiguration_task.cpp
+++ b/agent/src/beerocks/slave/tasks/ap_autoconfiguration_task.cpp
@@ -695,6 +695,12 @@ bool ApAutoConfigurationTask::send_ap_autoconfiguration_search_message(
             return false;
         }
         freq_band = ieee1905_1::tlvAutoconfigFreqBand::IEEE_802_11_6_GHZ;
+    } else if (radio->wifi_channel.get_freq_type() == beerocks::eFreqType::FREQ_5GL) {
+        LOG(DEBUG) << "Send Autoconfig Search for 5GL BAND: ";
+        freq_band = ieee1905_1::tlvAutoconfigFreqBand::IEEE_802_11_5_GHZ;
+    } else if (radio->wifi_channel.get_freq_type() == beerocks::eFreqType::FREQ_5GH) {
+        LOG(DEBUG) << "Send Autoconfig Search for 5GH BAND: ";
+        freq_band = ieee1905_1::tlvAutoconfigFreqBand::IEEE_802_11_5_GHZ;
     } else {
         LOG(ERROR) << "unsupported freq_type=" << int(radio->wifi_channel.get_freq_type())
                    << ", iface=" << radio_iface;
@@ -1110,6 +1116,8 @@ bool ApAutoConfigurationTask::add_wsc_m1_tlv(const std::string &radio_iface)
         cfg.bands = WSC::WSC_RF_BAND_2GHZ;
         break;
     case beerocks::FREQ_5G:
+    case beerocks::FREQ_5GL:
+    case beerocks::FREQ_5GH:
         cfg.bands = WSC::WSC_RF_BAND_5GHZ;
         break;
     case beerocks::FREQ_24G_5G:
@@ -1216,7 +1224,17 @@ void ApAutoConfigurationTask::handle_ap_autoconfiguration_response(
     if (discovery_status_it != m_discovery_status.end() && discovery_status_it->second.completed) {
         return;
     }
-    m_discovery_status[freq_type].completed = true;
+
+    if ((freq_type == beerocks::eFreqType::FREQ_5G) && (db->device_conf.num_of_bands == 4)){
+        LOG(DEBUG) << "Received Search response for 5G band in QUAD band device - Set 5GH and 5GL discovery status true" ;
+
+        m_discovery_status[beerocks::eFreqType::FREQ_5GL].completed = true;
+        m_discovery_status[beerocks::eFreqType::FREQ_5GH].completed = true;
+
+    } else {
+        m_discovery_status[freq_type].completed = true;
+    }
+
     db->statuses.controller_discovery_completed = true;
 
     LOG(DEBUG) << "received ap_autoconfiguration response for " << band_name << " band";
@@ -1297,8 +1315,6 @@ void ApAutoConfigurationTask::handle_ap_autoconfiguration_response(
     // Mark discovery status completed on band mentioned on the response and fill AgentDB fields.
     db->controller_info.prplmesh_controller = prplmesh_controller;
     db->controller_info.bridge_mac          = src_mac;
-    m_discovery_status[freq_type].completed = true;
-    db->statuses.controller_discovery_completed = true;
 
     LOG(DEBUG) << "controller_discovered on " << band_name
                << " band, controller bridge_mac=" << src_mac
diff --git a/agent/src/beerocks/slave/tlvf_utils.cpp b/agent/src/beerocks/slave/tlvf_utils.cpp
index 7a7abab2b..bd05d6d6b 100644
--- a/agent/src/beerocks/slave/tlvf_utils.cpp
+++ b/agent/src/beerocks/slave/tlvf_utils.cpp
@@ -163,6 +163,9 @@ bool tlvf_utils::add_ap_radio_basic_capabilities(ieee1905_1::CmduMessageTx &cmdu
                << beerocks::utils::convert_frequency_type_to_string(
                       radio->wifi_channel.get_freq_type())
                << "):";
+
+    LOG(DEBUG) <<"OPERATING CLASS FILLED FOR FREQ_BAND: " << radio->wifi_channel.get_freq_type();
+
     auto operating_classes = son::wireless_utils::get_operating_classes_of_freq_type(
         radio->wifi_channel.get_freq_type());
 
diff --git a/common/beerocks/bcl/include/bcl/beerocks_defines.h b/common/beerocks/bcl/include/bcl/beerocks_defines.h
index f7253f2b2..85822b768 100644
--- a/common/beerocks/bcl/include/bcl/beerocks_defines.h
+++ b/common/beerocks/bcl/include/bcl/beerocks_defines.h
@@ -91,7 +91,7 @@ enum eStructsConsts {
     VHT_MCS_SET_NL802_SIZE               = 32,
     HE_MCS_SET_SIZE                      = 12,
     HE_MCS_SET_NL802_SIZE                = 32,
-    DEV_MAX_RADIOS                       = 3,
+    DEV_MAX_RADIOS                       = 4,
     DSCP_MAPPING_LIST_LENGTH             = 64,
     CHIPSET_VENDOR_LENGTH                = 64,
 };
@@ -417,6 +417,8 @@ enum eFreqType : uint8_t {
     FREQ_58G      = 1 << 2,
     FREQ_24G_5G   = FREQ_24G | FREQ_5G,
     FREQ_6G       = 1 << 3,
+    FREQ_5GL      = 5,
+    FREQ_5GH      = 6,
     FREQ_AUTO     = 1 << 4,
     FREQ_UNKNOWN  = (uint8_t)~0,
 };
diff --git a/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h b/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
index c849abefe..c2763c06a 100644
--- a/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
+++ b/common/beerocks/bcl/include/bcl/son/son_wireless_utils.h
@@ -306,7 +306,7 @@ public:
         bool bSTA                                         = false;
         std::string backhaul_wps_ssid;
         std::string backhaul_wps_passphrase;
-        int freq_band                                     = -1;
+        beerocks::eFreqType freq_band = beerocks::eFreqType::FREQ_UNKNOWN; /**< Frequency band */
         std::string multi_ap;
         std::string old_iface_name;
         bool wps_pushbutton                               = false;
diff --git a/common/beerocks/bcl/source/beerocks_utils.cpp b/common/beerocks/bcl/source/beerocks_utils.cpp
index 8c7cc7e89..0f727da12 100644
--- a/common/beerocks/bcl/source/beerocks_utils.cpp
+++ b/common/beerocks/bcl/source/beerocks_utils.cpp
@@ -273,6 +273,10 @@ std::string utils::convert_frequency_type_to_string(beerocks::eFreqType freq_typ
         return "5GHz";
     case beerocks::FREQ_6G:
         return "6GHz";
+    case beerocks::FREQ_5GL:
+        return "5GlHz";
+    case beerocks::FREQ_5GH:
+        return "5GhHz";
     case beerocks::FREQ_AUTO:
         return "Auto Frequency";
     case beerocks::FREQ_UNKNOWN:
diff --git a/common/beerocks/bcl/source/beerocks_wifi_channel.cpp b/common/beerocks/bcl/source/beerocks_wifi_channel.cpp
index 61c4c9973..db8261e51 100644
--- a/common/beerocks/bcl/source/beerocks_wifi_channel.cpp
+++ b/common/beerocks/bcl/source/beerocks_wifi_channel.cpp
@@ -17,6 +17,8 @@ WifiChannel::WifiChannel() { initialize_empty_wifi_channel_members(); }
 WifiChannel::WifiChannel(uint8_t channel, uint16_t center_frequency, eWiFiBandwidth bandwidth,
                          bool ext_above_secondary)
 {
+    LOG(DEBUG) << "WIFI_CHANNEL CREATION WITH CENTER_FREQUENCY FOR CHANNEL:" << channel << " CENTER_FREQ:" << center_frequency << " BW:" << bandwidth << " ext_above_second: " << ext_above_secondary;
+
     beerocks::eFreqType freq_type = son::wireless_utils::which_freq_type(center_frequency);
 
     if (!are_params_valid(channel, freq_type, center_frequency, bandwidth)) {
@@ -51,6 +53,8 @@ WifiChannel::WifiChannel(uint8_t channel, uint16_t center_frequency, eWiFiBandwi
 WifiChannel::WifiChannel(uint8_t channel, eFreqType freq_type, eWiFiBandwidth bandwidth,
                          bool ext_above_secondary)
 {
+    LOG(DEBUG) << "WIFI_CHANNEL CREATION WITH FREQ_TYPE FOR CHANNEL:" << channel << " FREQ_TYPE: " <<freq_type << " BW:" << bandwidth << " ext_above_second: " << ext_above_secondary;
+
     uint16_t center_frequency = son::wireless_utils::channel_to_vht_center_freq(
         channel, freq_type, bandwidth, ext_above_secondary);
 
@@ -267,6 +271,7 @@ void WifiChannel::initialize_wifi_channel_members(uint8_t channel, eFreqType fre
 bool WifiChannel::is_central_channel(uint8_t channel, eWiFiBandwidth bandwidth,
                                      eFreqType freq_type) const
 {
+    // [NLHAL] TODO - FOR 5GH and 5GL
     if ((freq_type == eFreqType::FREQ_5G && bandwidth >= eWiFiBandwidth::BANDWIDTH_80) ||
         (freq_type == eFreqType::FREQ_6G && bandwidth >= eWiFiBandwidth::BANDWIDTH_40)) {
         if (son::wireless_utils::is_320MHz_channelization(freq_type, bandwidth)) {
@@ -314,7 +319,10 @@ bool WifiChannel::are_params_valid(uint8_t channel, eFreqType freq_type, uint16_
             return false;
         }
     } break;
-    case eFreqType::FREQ_5G: {
+    //[NLHAL] Use channels_table_5g for 5GL and 5GH band
+    case eFreqType::FREQ_5G:
+    case eFreqType::FREQ_5GL:
+    case eFreqType::FREQ_5GH: {
         if (!is_central_channel(channel, bandwidth, freq_type)) {
             auto channel_it = son::wireless_utils::channels_table_5g.find(channel);
             if (channel_it == son::wireless_utils::channels_table_5g.end()) {
diff --git a/common/beerocks/bcl/source/son/son_wireless_utils.cpp b/common/beerocks/bcl/source/son/son_wireless_utils.cpp
index 96683a336..f43ddd2ea 100644
--- a/common/beerocks/bcl/source/son/son_wireless_utils.cpp
+++ b/common/beerocks/bcl/source/son/son_wireless_utils.cpp
@@ -550,6 +550,9 @@ wireless_utils::get_operating_classes_of_freq_type(beerocks::eFreqType freq_type
     // and one past the last of the relevant operating classes
     decltype(operating_classes_list)::const_iterator first_iterator, last_iterator;
 
+    // [NLHAL] TODO - Removed opclass 128 to 130 from 5gl to prevent op class overlap with 5gh
+    std::list<uint8_t> radio_5gl = {115, 116, 117, 118, 119, 120};
+
     switch (freq_type) {
     case beerocks::eFreqType::FREQ_24G:
         first_iterator = operating_classes_list.find(81);
@@ -563,6 +566,16 @@ wireless_utils::get_operating_classes_of_freq_type(beerocks::eFreqType freq_type
         first_iterator = operating_classes_list.find(131);
         last_iterator  = operating_classes_list.end();
         break;
+    case beerocks::eFreqType::FREQ_5GH:
+        first_iterator = operating_classes_list.find(121);
+        last_iterator  = operating_classes_list.find(131);
+        break;
+    case beerocks::eFreqType::FREQ_5GL:
+        for (auto val : radio_5gl) {
+            op_classes.push_back(val);
+        }
+        return op_classes;
+
     default:
         LOG(ERROR) << "Unknown freq type " << int(freq_type) << ", must be 2.4GHz, 5GHz, or 6GHz";
         return {};
@@ -911,6 +924,8 @@ uint16_t wireless_utils::channel_to_freq(int channel, beerocks::eFreqType freq_t
             return (channel * 5) + 2407;
         break;
     case beerocks::FREQ_5G:
+    case beerocks::FREQ_5GL:
+    case beerocks::FREQ_5GH:
         if (channel < 7 || channel > 196) {
             LOG(ERROR) << "invalid 5ghz channel: " << channel;
             return 0;
@@ -1015,7 +1030,10 @@ int wireless_utils::channel_to_vht_center_freq(int channel, beerocks::eFreqType
             return 0;
         }
     } break;
-    case beerocks::FREQ_5G: {
+//[NLHAL] Use channels_table_5g for 5GL and 5GH band
+    case beerocks::FREQ_5G:
+    case beerocks::FREQ_5GL:
+    case beerocks::FREQ_5GH: {
         // treat 80+80MHz as 80MHz
         if (bandwidth == beerocks::eWiFiBandwidth::BANDWIDTH_80_80) {
             bandwidth = beerocks::eWiFiBandwidth::BANDWIDTH_80;
@@ -1750,8 +1768,10 @@ std::list<uint8_t> wireless_utils::string_to_wsc_oper_class(const std::string &o
     if (operating_class == "5gh") {
         return {121, 122, 123, 124, 125, 126, 127, 128, 129, 130};
     }
+
+    // [NLHAL] TODO - Removed opclass 128 to 130 from 5gl to prevent op class overlap with 5gh
     if (operating_class == "5gl") {
-        return {115, 116, 117, 118, 119, 120, 128, 129, 130};
+        return {115, 116, 117, 118, 119, 120};
     }
     if (operating_class == "5g") {
         return radio_5g;
diff --git a/common/beerocks/bwl/include/bwl/nl80211_client.h b/common/beerocks/bwl/include/bwl/nl80211_client.h
index 7e4db7a62..acaa1f8d3 100644
--- a/common/beerocks/bwl/include/bwl/nl80211_client.h
+++ b/common/beerocks/bwl/include/bwl/nl80211_client.h
@@ -365,6 +365,7 @@ public:
          */
         std::vector<band_info> bands;
 	struct eht_info eht_cfg;
+        int8_t num_of_bands;
     };
 
 
@@ -630,6 +631,7 @@ public:
     virtual bool get_mlo_active_link_count(std::string &bss,int8_t &active_link) =0;
     virtual bool get_sta_iface_mac(const std::string &sta_iface,
                                              std::string &sta_mac) = 0 ;
+    virtual bool get_radio_band_info(int8_t &num_of_bands) = 0;
 };
 
 } // namespace bwl
diff --git a/common/beerocks/bwl/include/bwl/nl80211_client_impl.h b/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
index d08b5fcf1..d4ed61439 100644
--- a/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
+++ b/common/beerocks/bwl/include/bwl/nl80211_client_impl.h
@@ -204,6 +204,8 @@ public:
 
     virtual bool get_sta_iface_mac(const std::string &sta_iface,
                                              std::string &sta_mac) override ;
+   
+    virtual bool get_radio_band_info(int8_t &num_of_bands) override;
 private:
     /**
      * NL80211 socket to send messages and receive responses to/from the WiFi driver.
diff --git a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
index 4f03bbadf..ee2215ddb 100644
--- a/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/ap_wlan_hal_nl80211.cpp
@@ -372,6 +372,8 @@ prplmesh::hostapd::Configuration ap_wlan_hal_nl80211::load_hostapd_config(const
                 case beerocks::FREQ_24G: band_match = (chan_start >= 1 && chan_end <= 14); break;
                 case beerocks::FREQ_5G: band_match = (chan_start >= 36 && chan_end <= 177); break;
                 case beerocks::FREQ_6G: band_match = (chan_start >= 1 && chan_end <= 233); break;
+                case beerocks::FREQ_5GL: band_match = (chan_start >= 36 && chan_end <= 65); break;
+                case beerocks::FREQ_5GH: band_match = (chan_start >= 100 && chan_end <= 177); break;
                 default: break;
             }
 
@@ -754,8 +756,9 @@ bool ap_wlan_hal_nl80211::refresh_radio_info()
     }
 #endif
 
+    nl80211_client::interface_info if_info;
+
     if (m_radio_info.frequency_band == beerocks::eFreqType::FREQ_UNKNOWN) {
-        nl80211_client::interface_info if_info;
         //TODO: PRPLMESH_NLHAL To be updated the logic for MLO Link
         if(!m_nl80211_client->get_interface_info(get_iface_name(), if_info)){
             LOG(ERROR) << "Unable to read interface info of " << m_radio_info.iface_name;
@@ -772,11 +775,16 @@ bool ap_wlan_hal_nl80211::refresh_radio_info()
             return false;
         }
 
-        m_radio_info.frequency_band = freq_type;
+        LOG(DEBUG) << "NUMBER_OF_BANDS IN RADIO: " << radio_info.num_of_bands << " freq band: " << freq_type ;
 
-        LOG(INFO) << "freq band: " << freq_type ;
+        m_radio_info.frequency_band = freq_type;
     }
 
+    auto freq_band = m_radio_info.frequency_band;
+
+    if ((m_radio_info.frequency_band ==  beerocks::eFreqType::FREQ_5GL) || (m_radio_info.frequency_band ==  beerocks::eFreqType::FREQ_5GH)){
+        freq_band = beerocks::eFreqType::FREQ_5G;
+    }
 
     if (radio_info.bands.begin()->supported_channels.empty()) {
         LOG(ERROR) << "Supported channels map is empty";
@@ -785,7 +793,7 @@ bool ap_wlan_hal_nl80211::refresh_radio_info()
     auto band_info_it =
         std::find_if(radio_info.bands.begin(), radio_info.bands.end(),
                      [&](const bwl::nl80211_client::band_info &b) {
-                         return m_radio_info.frequency_band ==
+                         return freq_band ==
                                 son::wireless_utils::which_freq_type(
                                     b.supported_channels.begin()->second.center_freq);
                      });
@@ -841,13 +849,29 @@ bool ap_wlan_hal_nl80211::refresh_radio_info()
                m_radio_info.bsta_maximum_links = 3;
     }
 
+    if ((m_radio_info.frequency_band == beerocks::eFreqType::FREQ_5G) && (radio_info.num_of_bands == 4)) {
+        LOG(DEBUG) << "QUAD BAND RADIO - Need to check 5GH or 5GL for 5G BAND";
+
+        constexpr uint32_t BAND_5GL_MIN_FREQ  = 5150U;
+        constexpr uint32_t BAND_5GL_MAX_FREQ  = 5350U;
+
+        if ((if_info.frequency >= BAND_5GL_MIN_FREQ) && (if_info.frequency <= BAND_5GL_MAX_FREQ)){
+            LOG(DEBUG) << "5G Low Band:";
+            m_radio_info.frequency_band = beerocks::eFreqType::FREQ_5GL;
+        } else {
+            LOG(DEBUG) << "5G High Band:";
+            m_radio_info.frequency_band = beerocks::eFreqType::FREQ_5GH;
+        }
+    }
 
     // update radio_id in m_radio_info from prplmesh config
     int prpl_radio_idx = -1;
     switch (m_radio_info.frequency_band) {
         case beerocks::FREQ_24G: prpl_radio_idx = 0; break;
         case beerocks::FREQ_5G: prpl_radio_idx = 1;  break;
+        case beerocks::FREQ_5GL: prpl_radio_idx = 1;  break;
         case beerocks::FREQ_6G: prpl_radio_idx = 2;  break;
+        case beerocks::FREQ_5GH: prpl_radio_idx = 3;  break;
         default: break;
     }
 
@@ -861,6 +885,8 @@ bool ap_wlan_hal_nl80211::refresh_radio_info()
 
     LOG(INFO) << "RADIO_ID: " << m_radio_info.radio_id;
 
+    LOG(INFO) << "updated freq band: " << m_radio_info.frequency_band ;
+
     return base_wlan_hal_nl80211::refresh_radio_info();
 } // namespace nl80211
 
@@ -2906,6 +2932,8 @@ void ap_wlan_hal_nl80211::generate_bssid_pool()
         case beerocks::FREQ_24G: radio_id = 0; break;
         case beerocks::FREQ_5G: radio_id = 1;  break;
         case beerocks::FREQ_6G: radio_id = 2;  break;
+        case beerocks::FREQ_5GL: radio_id = 1;  break;
+        case beerocks::FREQ_5GH: radio_id = 3;  break;
         default: break;
     }
 
diff --git a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
index 4cd26507b..d1cd8eb79 100644
--- a/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
+++ b/common/beerocks/bwl/nl80211/base_wlan_hal_nl80211.cpp
@@ -727,7 +727,7 @@ bool base_wlan_hal_nl80211::refresh_radio_info()
         }
 
         //center freq = 5 GHz + (5 * index)
-        if (m_radio_info.frequency_band == beerocks::eFreqType::FREQ_5G) {
+        if ((m_radio_info.frequency_band == beerocks::eFreqType::FREQ_5G) || (m_radio_info.frequency_band == beerocks::eFreqType::FREQ_5GL) || (m_radio_info.frequency_band == beerocks::eFreqType::FREQ_5GH)) {
             iter = reply.find("vht_oper_centr_freq_seg0_idx");
             if (iter != reply.end()) {
                 auto freq_idx = beerocks::string_utils::stoi(iter->second);
diff --git a/common/beerocks/bwl/shared/nl80211_client_impl.cpp b/common/beerocks/bwl/shared/nl80211_client_impl.cpp
index 4daef7229..18e4b5335 100644
--- a/common/beerocks/bwl/shared/nl80211_client_impl.cpp
+++ b/common/beerocks/bwl/shared/nl80211_client_impl.cpp
@@ -934,6 +934,30 @@ bool nl80211_client_impl::get_radio_info(const std::string &interface_name, radi
 		  }
 	  }
 
+          if (tb[NL80211_ATTR_WIPHY_RADIOS]) {
+             struct nlattr *radio;
+             int rem_radios;
+             radio_info.num_of_bands = 0;
+
+             nla_for_each_nested(radio, tb[NL80211_ATTR_WIPHY_RADIOS], rem_radios) {
+             struct nlattr *radio_prop;
+             int rem_radio_prop;
+             nla_for_each_nested(radio_prop, radio, rem_radio_prop) {
+
+                        switch (nla_type(radio_prop)) {
+                        case NL80211_WIPHY_RADIO_ATTR_INDEX:
+                                LOG(DEBUG) << "RADIO IDX: " << nla_get_u32(radio_prop);
+                                radio_info.num_of_bands++;
+                                break;
+                          default:
+                                break;
+                          }
+                  }
+              }
+              LOG(DEBUG) << "NUMBER OF BANDS: " << radio_info.num_of_bands;
+          }
+
+
         });
 }
 
@@ -2015,4 +2039,59 @@ bool nl80211_client_impl::get_sta_iface_mac(const std::string &sta_iface,
         });
 }
 
+bool nl80211_client_impl::get_radio_band_info(int8_t &num_of_bands)
+{
+
+    num_of_bands = 0;
+    if (!m_socket) {
+        LOG(ERROR) << "Socket is NULL!";
+        return false;
+    }
+
+    return m_socket.get()->send_receive_msg(
+        NL80211_CMD_GET_WIPHY, NLM_F_DUMP,
+        [&](struct nl_msg *msg) -> bool {
+            nla_put_flag(msg, NL80211_ATTR_SPLIT_WIPHY_DUMP);
+
+            return true;
+        },
+        [&](struct nl_msg *msg) {
+            struct nlattr *tb[NL80211_ATTR_MAX + 1];
+            struct genlmsghdr *gnlh = static_cast<genlmsghdr *>(nlmsg_data(nlmsg_hdr(msg)));
+
+            // Parse the netlink message
+            if (nla_parse(tb, NL80211_ATTR_MAX, genlmsg_attrdata(gnlh, 0), genlmsg_attrlen(gnlh, 0),
+                          NULL)) {
+                LOG(ERROR) << "Failed to parse netlink message!";
+                return;
+            }
+
+          if (tb[NL80211_ATTR_WIPHY_RADIOS]) {
+
+             struct nlattr *radio;
+              int rem_radios;
+                num_of_bands = 0;
+
+                nla_for_each_nested(radio, tb[NL80211_ATTR_WIPHY_RADIOS], rem_radios) {
+                struct nlattr *radio_prop;
+                int rem_radio_prop;
+                nla_for_each_nested(radio_prop, radio, rem_radio_prop) {
+
+                        switch (nla_type(radio_prop)) {
+                        case NL80211_WIPHY_RADIO_ATTR_INDEX:
+                                LOG(DEBUG) << "RADIO IDX: " << nla_get_u32(radio_prop);
+                                num_of_bands++;
+                                break;
+                        default:
+                                break;
+                        }
+                  }
+              }
+              LOG(DEBUG) << "NUMBER OF BANDS: " << num_of_bands;
+          }
+
+        });
+}
+
+
 } // namespace bwl
diff --git a/common/beerocks/scripts/prplmesh_utils.sh.in b/common/beerocks/scripts/prplmesh_utils.sh.in
index 676149530..8bc91d8b2 100755
--- a/common/beerocks/scripts/prplmesh_utils.sh.in
+++ b/common/beerocks/scripts/prplmesh_utils.sh.in
@@ -451,7 +451,8 @@ status_function() {
         [ -L "$main_agent_log" ] || return
 
         grep -q "[^[:print:]]" "$main_agent_log" && err "$main_agent_log contains non-printable characters!"
-	    radios=$(grep -a 'fronthaul_iface=' "$main_agent_log" | head -n 3 | sed -n 's/.*fronthaul_iface=\([^ ]*\).*/\1/p')
+        radio_count=$(uci show prplmesh | grep '=wifi-device' | wc -l)
+        radios=$(grep -a 'fronthaul_iface=' "$main_agent_log" | head -n "$radio_count" | sed -n 's/.*fronthaul_iface=\([^ ]*\).*/\1/p')
         for radio in $radios ; do
             report "$radio radio agent operational" radio_agent_operational "$LOGS_PATH" "$radio"
         done
diff --git a/controller/src/beerocks/bml/bml_defs.h b/controller/src/beerocks/bml/bml_defs.h
index 60d1d3f36..2fcb9a0eb 100644
--- a/controller/src/beerocks/bml/bml_defs.h
+++ b/controller/src/beerocks/bml/bml_defs.h
@@ -46,7 +46,7 @@ extern "C" {
 
 /* BML Node Definitions */
 #define BML_NODE_NAME 64      /* Node hostname */
-#define BML_NODE_MAX_RADIOS 3 /* Maximal number of radios */
+#define BML_NODE_MAX_RADIOS 4 /* Maximal number of radios */
 #define BML_NODE_MAX_VAPS 16  /* Maximal number of VAPs */
 #define BML_NODE_IFACE_NAME_LEN                                                                    \
     (32 +                                                                                          \
diff --git a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
index fcfce00be..6ca713dba 100644
--- a/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
+++ b/framework/platform/bpl/cfg/uci/bpl_cfg_wifi.cpp
@@ -285,7 +285,6 @@ bool bpl_cfg_get_wireless_settings(std::list<son::wireless_utils::sBssInfoConf>
         std::string hwmode;
         if (!uci_get_option(package_name, "wifi-device", device, "hwmode", hwmode)) {
             LOG(DEBUG) << "Failed to get 'hwmode' from section " << device;
-            continue;
         }
         // Operating class is updated based on band info present in config since hwmode for 6g and 5g are same
 #if 0
@@ -325,7 +324,21 @@ bool bpl_cfg_get_wireless_settings(std::list<son::wireless_utils::sBssInfoConf>
         }
 
         LOG(DEBUG) << "band: " << radio_band;
-      
+     
+        if (radio_band == "5g") {
+            std::string channel_range;
+            if (!uci_get_option(package_name, "wifi-device", device, "channels", channel_range)) {
+                LOG(DEBUG) << "Failed to get 'band' from section " << device;
+                continue;
+            }
+            LOG(DEBUG) << " 5G BAND Channel_range: " << channel_range;
+            if (channel_range == "36-64") {
+                radio_band = "5gl";
+            } else if (channel_range == "100-165") {
+                radio_band = "5gh";
+            }
+        }
+ 
         configuration.operating_class.splice(
           configuration.operating_class.end(),
           son::wireless_utils::string_to_wsc_oper_class(radio_band));
-- 
2.34.1

